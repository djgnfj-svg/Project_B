# Godot engine binary (too large for GitHub — each dev keeps their own)
Godot_v*.exe
*.exe

# Godot 4+ editor cache / generated
.godot/
.import/
*.translation

# Export artifacts — export_presets.cfg는 커밋한다(웹 프리셋뿐, 비밀값 없음 — 클론만으로 익스포트 가능하게).
# 모바일 프리셋(서명 비밀번호)을 추가하게 되면 그때 다시 분리할 것.
export.cfg
/build/
/export/

# Claude Code personal overrides (keep shared settings.json, ignore local)
.claude/settings.local.json

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# 검수·테스트 잔재 (리드 작업용 임시 산출물 — 웹 익스포트 pck 오염 방지)
scratch_*
*.log


.mcp.json
server/game-worker/public/
.wrangler/
**/.wrangler/
_holdout/
