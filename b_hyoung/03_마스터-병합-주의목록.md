# 마스터 병합 주의 목록 (인수인계)

> `feat/MonsterAnima` → `master` 병합 **전에 반드시 확인·정리**할 것들.
> 임시/테스트용으로 만들어둔 것, 병합 시 문제 될 것들. 위에서부터 우선순위.

---

## 🔴 반드시 되돌려야 (안 하면 테스트/게임 깨짐)

### 1. `data/chapters/chapter1.tres` — 보스 직행 임시 축소
- **현재**: 칸 1개(보스만) — 보스 빨리 테스트하려고 축소함.
- **원래**: 칸 4개 = 전투3 + 모닥불1 (마지막=보스).
- **증상**: `test_game_state_auto`가 **빨강**("챕터1 칸 수 = 4" FAIL 등). 정상 챕터 진행(로비→마을→스테이지1→…→보스)도 안 됨.
- **조치**: 원래 4칸 구성으로 복원. (git에서 이전 버전 참고: `git show HEAD:data/chapters/chapter1.tres`)

### 2. `project.godot`의 `main_scene` 확인
- 테스트 중 `main_scene`을 `stage_boss.tscn`/`village.tscn`으로 임시 스왑하고 되돌리는 패턴을 씀.
- **병합 전 반드시 `res://src/main/main.tscn`인지 확인**. (현재 워킹 트리는 정상 복원돼 있을 것 — 매번 `cp` 백업 후 복원했음. 그래도 재확인.)

---

## 🟡 정체 확인 필요 (실험/미배선)

### 3. `src/enemies/boss_new.gd` (+ `.uid`) — 미배선 실험 골격
- 어느 씬/데이터에도 안 물린 빈 보스 템플릿(악어와 같은 계약만 지키는 스캐폴드).
- **판단**: 실제 쓸 거면 유지, 아니면 병합 전 삭제. 지금은 무해(참조 0)하지만 죽은 코드.

### 4. 망령 vs 악어 보스 — 어느 걸 정본으로?
- `stage_boss.tscn`이 현재 **망령**(`wraith_boss.tres`) 참조. 악어(`croc_boss.tres`·`boss_croc.gd`·croc 스프라이트)는 **안 지웠고 그대로 살아있음**.
- 망령은 boss_croc.gd 엔진을 그대로 씀(카이팅만 데이터로 추가). 즉 **엔진 1개, 데이터 2개** 공존.
- **판단**: 망령을 챕터1 보스로 확정할지, 악어로 되돌릴지 결정. 되돌리려면 stage_boss.tscn의 def만 croc로 스왑(파일 삭제 불필요).

---

## 🟢 커밋에서 빼야 할 쓰레기

### 5. `assets/sprites/stage/boss_arena.png 2.import` — macOS 중복 파일
- 이전 pull/복사 때 생긴 " 2" 중복 쓰레기(진짜 파일 `boss_arena.png`·`boss_arena.png.import`은 이미 추적됨).
- **조치**: 삭제(이 세션에서 커밋 전 삭제 처리함). 병합 시 재발 없어야 함.
- ⚠ macOS에서 pull 후 " 2" 붙은 파일이 또 생기면 원본과 byte 동일한지 확인 후 삭제.

---

## ⚪ 병합 후 실기 검증 체크리스트 (웹 2클라)

정식 챕터(위 1번 복원) 기준으로:
- [ ] 로비→마을→게이트 F→스테이지1→…→보스 정상 진행
- [ ] 코옵 6패턴 순환 + 메테오 판정(안전지대 밖 즉사, 유예 0.3s)
- [ ] 네트워크 지연 하 게스트가 억울하게 안 죽는지 (net_anchor 수정 효과)
- [ ] 설정→마을로 가기(스테이지·호스트만) / 직업 변경(마을만)
- [ ] 2클라 직업 변경 상대 화면 반영 + 스테이지에선 잠김
- [ ] 마을 버전 오버레이 `v0725g-menu` 이상

## ⚪ 하네스 문서 갱신 (병합 시)
- `CLAUDE.md` 변경 이력 표에 이번 작업(망령 보스·코옵 6패턴·메테오 판정·지연보정·설정 메뉴) 추가.
- `projectb-verify §1` 테스트 스위트 목록은 변화 없음(새 `*_auto.gd` 안 만듦).
- `b_hyoung/` 폴더는 인수인계용 — 병합 시 유지할지 지울지 판단(정식 문서는 CLAUDE.md/GDD).
