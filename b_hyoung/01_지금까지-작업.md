# 지금까지 작업한 것 (인수인계)

> 브랜치 `feat/MonsterAnima` · 최신 배포 빌드 **`v0725g-menu`**
> 배포 주소(임시): https://projectb-game.youqlrqod.workers.dev/?host (게스트: `?join=방코드`)
> 릴레이: 공용 `wss://relay.jachana.com` (자동)

이 문서 = **이번 세션(맥)에서 새로 한 것**. 정식 변경 이력은 `CLAUDE.md` 표가 정본.

---

## 1. 망령 보스(늪의 군주 대체 실험) + 카이팅

- **새 보스 = `data/enemies/wraith_boss.tres`** (`BossDef`). max_hp 360, 3패턴(부채꼴 스윙 range130 · 슬램 range80 늪 · 물뿌리기 burst6), body_radius 28, **keep_distance 150(카이팅)**.
- **스프라이트** = `assets/sprites/enemies/boss_wraith*.png` (idle/cast/swing/death) + `boss_wraith_frames.tres`(SpriteFrames). 소스 = `assets/aseprite/boss_wraith_box_64.aseprite`.
- **카이팅 로직**: `src/core/boss_def.gd`에 `keep_distance` 필드 추가(>0이면 카이팅). `src/enemies/boss_croc.gd` CHASE가 keep_distance>0이면 플레이어와 거리 유지(가까우면 물러남). **악어(croc=0)는 영향 없음** — 데이터로만 갈림.
- **배선**: `src/stage/stage_boss.tscn`에서 보스 def를 악어→망령으로 교체(id 4).
- ⚠ **악어 파일(boss_croc.gd/tscn·croc_boss.tres·croc 스프라이트)은 안 지웠다** — 참조만 스왑. 되돌리려면 stage_boss.tscn의 def만 croc로.
- `src/enemies/boss_new.gd` = **미배선 실험용 보스 골격**(악어와 같은 계약만 지키면 되는 빈 템플릿). 아직 아무 씬에도 안 물림.

## 2. 코옵 파훼 패턴 6종 (핵심 신규 — `src/stage/coop_authority.gd`, 704줄)

보스전 중 **2인 협동 파훼 기믹**을 순서대로 순환 발동. 각 패턴 3·2·1 카운트다운(이름+설명) 후 시작.
보스 엔진 무접촉·**독립 병렬 컨트롤러**(stage_boss.tscn에 CoopAuthority 노드로 붙음).

| # | 패턴 | 유형 | 파훼 |
|---|---|---|---|
| 1 | 소울 케이지 | 속박+구출 | 한 명 갇힘+발밑 즉사장판 → 파트너가 근접 WASD로 구출 |
| 2 | 별의 낙하 | 이동 | 초록 표식 안에서 버티기 |
| 3 | 봉인진 | 키패드 | WASD 4키 순서 |
| 4 | 분산 | 이동 | 각자 다른 표식(초록A/주황B) |
| 5 | 뭉치기 | 이동 | 서로 가까이(초록 링) |
| 6 | 직면 | 키패드 | 1키 |

### 판정 모델 (중요 — 두 번 뒤집었다)
- **"닿으면 성공"이 아니라 "제한시간 끝까지 버티기"**. 만료 순간에만 판정.
- 만료 시 **조건 미충족자 머리 위로 메테오 낙하 → 즉사**(FAIL_DAMAGE 9999). 전원 충족 = 성공(메테오 없음).
- **마지막 1.5초 붉은 예고 고조**(화면 가장자리 맥동) + 만료 시 화면 플래시.
- 케이지: 지각 구출 시 갇힌 자에게 메테오.

### 네트워크 지연 보정 (사용자가 "맞을 거 안 맞는다" 지적 → 수정함)
- **버그였다**: 판정이 원격 플레이어의 `global_position`(보간 표시 좌표)을 봐서 이중 지연 → 억울한 사망.
- **수정**: 판정 좌표를 `net_anchor()`(최신 수신 좌표, 전투 판정과 동일 rules §3)로 교체.
- **유예 래치 `GRACE_S=0.3s`** + **반경 마진 `+12px`** 추가 — 지터 흡수.

### 이펙트 아트 (Aseprite MCP, 망령 테마 5톤)
`assets/sprites/fx/coop_*.png` 9종: marker·star·cage·seal·huddle·gaze·meteor·impact·doom.
전부 **표시 전용·네트워크 0**(호스트가 죽을 피어 목록만 브로드캐스트, 연출은 각 클라 로컬).

### 배선한 곳
- `src/core/net_schema.gd` — G_COOP_CALL/IN/RES 상수
- `src/core/event_bus.gd` — coop_call·coop_result 시그널
- `src/player/player.gd` — `bound` 플래그(속박 시 이동 정지)
- `src/stage/stage_boss.tscn` — CoopAuthority 노드(boss_eid="boss1")

## 3. 설정 메뉴 확장 — "마을로 가기" + "직업 변경"

- `src/ui/settings_panel.tscn/.gd` — 볼륨/음소거 아래 버튼 2개.
- **🏘 마을로 가기**: 스테이지에서만 보임, **호스트만**(씬 전환=호스트 권한). 확인창 → 예 = 파티 전원 마을 귀환.
- **🧑 직업 변경**: **마을에서만** 활성. 직업 3종 목록 → 내 캐릭터만 교체+재공지. 장비/골드 유지.
- **배선**: event_bus 시그널 2종(`leave_to_village_requested`·`job_change_requested`), `scene_flow.gd`(마을 귀환 수신, 호스트 가드), `peer_sync.gd`(직업 변경 수신·**마을 씬만** G_JOB 잠금 해제 — 스테이지에선 스탯 취사선택 악용 방지로 잠금 유지), `village.gd`(add_to_group("village")로 "마을인지" 판별).

---

## ⚠ PC에서 이어서 하기 전에 알아둘 것

1. **`data/chapters/chapter1.tres`가 보스 직행으로 임시 축소돼 있다**(칸 4→1). 보스만 빨리 테스트하려던 값. **`test_game_state_auto`를 깨뜨린다**. 정식 병합 전 원래 4칸(전투3+모닥불1)으로 복원 필요. → 로비 정상 흐름(로비→마을→게이트 F)으로 테스트하려면 이것부터 되돌리기.
2. **`.mcp.json`은 gitignore됨**(개인 설정). PC엔 그 머신용 Aseprite/Godot 경로로 `.mcp.json`을 따로 만들어야 MCP가 붙는다.
3. **헤드리스 테스트 현황**: combat_math·health·save 그린. game_state는 위 1번 때문에 빨강(코옵과 무관).
4. **보스 8방향 리디자인은 품질 이슈로 보류 중**(CLAUDE.md 이력). 커밋된 2방향 리셰이드 유지.
5. 실기 검증은 **웹 2클라**(브라우저 두 탭 `?host`+`?join=`)에서 사용자 몫. 마을 좌상단 버전이 `v0725g-menu`여야 최신.

## 검증/배포 명령 (맥 기준)
```bash
# 헤드리스 유닛 테스트
/Applications/Godot.app/Contents/MacOS/Godot --headless --path . -s res://tests/test_combat_math_auto.gd

# 특정 씬 부팅 컴파일 검증 (project.godot main_scene 임시 스왑 후 --quit-after)
# 웹 배포 (계정 자동 분기)
GODOT=/Applications/Godot.app/Contents/MacOS/Godot bash scripts/deploy_web.sh
```
> PC(윈도)에선 루트 `Godot_v4.7.1-stable_win64.exe`, 테스트는 **Bash 툴에서**(PowerShell은 자식 stdout 안 보임 — CLAUDE.md).
