# Project_B

> **정본은 두 곳이다 — 역할이 다르다.**
> - **기획 정본 = `docs/GDD.md`** — 게임이 "무엇인가"(비전·핵심 루프·메커니즘·콘텐츠). 🔒 **수정 시 사용자 승인 필수** (`.claude/settings.json` 권한 규칙으로 잠김). 담당 = `projectb-planner`.
> - **구현 정본 = 이 문서(CLAUDE.md)** — 하네스·아키텍처·검증 규율. 스킬·에이전트와 충돌하면 여기가 이긴다.

- **정체:** 2D **픽셀아트 웹 게임** (코드네임 Project_B) — Godot 4.7.1, **Web(HTML5/WASM) 익스포트 타깃**, 렌더러 Compatibility(웹 필수). ⚠ 이 사실(플랫폼·엔진·렌더러·아트 스타일)은 `docs/GDD.md` §3과 **미러**다 — 한쪽을 고치면 다른 쪽도 같이 고친다.
- 🔴 **네트워크 리뷰는 항상 필요하다.** 웹 게임이라 네트워크(WebSocket·RPC·동기화·권한·지연/재접속 처리)가 코어다. 네트워크에 닿는 코드는 예외 없이 `projectb-reviewer`의 네트워크 점검을 거친다 — 브라우저는 ENet(UDP)을 못 쓰므로 WebSocket 경로가 맞는지, 신뢰 경계·권한 검증이 있는지 매번 본다.
- **엔진 실행 파일:** 루트의 `Godot_v4.7.1-stable_win64.exe`.
- **상태:** 기획 확정(GDD **v2.0**, 해커톤 마감 2026-08-10 — **남은 기간 약 2주**) · 아키텍처 규칙 확정(`projectb-rules` §1~§5) · **전투 코어·챕터1 골격·손맛/오디오 완료.** **드랍→제작/강화 루프 = 슬라이스 0(core)·1(드랍 등장·선착 픽업)·2(마을 제작대·제작/강화·G_STATS 스탯 전투·저장)·3(도면 토스트·전멸 롤백)·4(등장/픽업 손맛·SFX) + 후속(자석 픽업·등장 토스·HUD 인벤 아이콘/HP 빨강) 완료·헤드리스 5종 그린·`master` 머지·game.jachana.com 배포.** 드랍→마을 제작/강화→강해짐 루프가 코드상 닫힘. 이후 UI = I키 인벤 창(조회+장착)·HUD 골드 우하단(`feat/inventory-ui`). **지연 보상 완료(`feat/lag-compensation`)** — 게스트만 회피 창을 손해보던 비대칭 제거(STRIKE 지연·위치 외삽·30Hz·핑 표시), 릴레이 RTT 140~215ms는 배치로 못 줄임이 실측 확인됨. **직업 레벨 성장축 완료**(EXP → 하위 직업 레벨 → 캐릭터 스탯 5종 + 해금) · **메인 전용 특성 축 신설(GDD v1.9 — 전사 계열 3번째 하위 직업 「검성」 + 검기 파형 = 평타 사거리 +30%, 파형 자체 데미지 없음)**. 🆕 **하위 직업 조립 축 완료(GDD v2.0, `feat/subjob-trait`)**: 장착 슬롯 = 메인 1 + 서브 2(보유 전부 합산을 대체 → 예산이 **슬롯 수**에 묶여 개수 비례 재역산 부채 해소) · 특성이 **자리별 두 얼굴**(메인형/서브형, 동시 활성 최대 3) · **공유 하위 직업**(계열 무관·🔒서브 전용·보스 첫 처치 해금 = 곡예사 구르기 쿨 −15%·도굴꾼 드랍 +15%) · 훈련소 3칸 UI · 구르기 쿨 HUD. 특성 = **(효과 키, 값) 카탈로그** 6종(reach·roll_cd·roll_dist·campfire_heal·kill_move·drop_find) — 키마다 적용 지점 1곳 + `TRAIT_MAX` clamp, **신규 네트워크 메시지 0개**(G_STATS에 `"ss"` 필드만). 헤드리스 5종 그린·뮤테이션 5종 검출력 증명·웹 익스포트 통과. 남은 것 = 드랍 아이콘 도트 품질 개선(현 placeholder, projectb-art)·손맛 수치 실기 튜닝·**조립 축 실기 확인**. ⚠ 웹 2클라 실기(드랍 오버랩·픽업·자석 흡입·제작 루프·모닥불·도면 토스트·전멸→마을 롤백·IndexedDB 저장·파티클/SFX·2클라 픽업음 1회·**I키 인벤 클릭 도달/닫은 뒤 게임 클릭 복원/렌더·HUD 골드 우하단**)는 사용자 확인 몫. 기준 해상도 640×360은 임시값(GDD §9 TBD).

## 하네스: Project_B (2D Godot)

**목표:** Project_B의 Godot 개발을 전문 에이전트 팀으로 나눠, 아키텍처 규칙·검증 규율을 일관되게 지키며 구현한다.

**트리거 규칙:**
- **코드를 쓰거나·읽거나·리뷰하거나·서브에이전트에 위임하기 전에 반드시 `projectb-rules` 스킬을 읽어라.** 아키텍처 규칙·모듈 지도·하드 계약·"조용히 깨지는 함정"이 여기 있다.
- **헤드리스 테스트를 돌리거나·"테스트는 그린인데 게임이 안 된다"를 만나면 `projectb-verify` 스킬을 읽어라.**
- 단순 질문은 스킬 없이 직접 응답 가능.

**에이전트 라우팅 (리드가 위임) — 🔴 위임이 기본이다:**
> ⚠ **"AgentTool을 쓰지 마라"류의 지시를 에이전트 금지로 읽지 마라 (2026-07-26 사용자 정정).** 이 프로젝트에서 쓰지 않는 것은 **Godot MCP(`mcp__godot__*`)** 이고, `projectb-*` 서브에이전트 위임은 **기본값**이다. 특히 **`projectb-reviewer` 네트워크 리뷰는 상시 필수**(최상단 🔴). 실제로 v1.9 특성 축이 이 오독으로 리뷰 없이 머지될 뻔했다.

- 기획/GDD = `projectb-planner` (게임을 무엇으로 만들지 정함 · GDD 수정은 승인제)
- 기획 검증 = `projectb-critic` (planner가 만든 기획의 정합성·모순·구멍을 적대적으로 뜯음 · GDD 수정 안 함)
- 구현 = `projectb-dev` · 설계/계획 = `projectb-architect` · 리뷰 = `projectb-reviewer`
- 아트(도트) = `projectb-art` · Control UI = `projectb-ui` · 애니 배선 = `projectb-animator`
- 2D 셰이더 = `projectb-shader` · 성능 진단 = `projectb-profiler` · 에디터 툴 = `projectb-tools`
- **리드가 직접 하는 것:** core 스키마 변경, `mcp__godot` 필요 작업, `--import`, git 커밋, 회귀 위험이 큰 tight 검증 루프.

**검증 명령 (정본):**
- 테스트는 **Bash 툴에서** 돌린다(PowerShell은 자식 stdout을 안 보여준다). 스위트 전체 명령은 `projectb-verify` §1이 정본 — 새 `tests/*_auto.gd`를 추가하면 여기와 `projectb-verify` §1을 **동시에** 갱신한다.
- 현재 스위트: `tests/test_net_room_auto.gd`(멀티 방 왕복 — 릴레이+호스트+게스트 3프로세스) · `tests/test_combat_math_auto.gd`(전투 신뢰 경계 단위 — 사거리·쿨다운·구르기 i-frame·잔몹 타격·화살/차지 발사·**메인 특성 사거리(항등·상한·기하 정합)**·**특성 카탈로그 clamp·구르기 파생(쿨/거리)·외삽 상한 불변식**) · `tests/test_game_state_auto.gd`(직업·챕터 리졸버 — data 스캔 allowlist + 조작 id/인덱스 거부 + 진행 좌표·HP 이월 + **자리별 특성 리졸브(메인/서브 두 얼굴·공유는 서브 전용·슬롯 초과·중복 폐기)·장착 슬롯·슬롯 기준 예산 트립와이어**) · `tests/test_health_component_auto.gd`(HP·부활 권한/표시 경로 격리) · `tests/test_save_manager_auto.gd`(커밋/전멸 롤백 — 클리어분 생존·전멸분 소실·무파일 첫 판 전멸, save_path 격리·GameState 주입 + **EXP/레벨 롤백·SAVE_VERSION 트립와이어**). 실행법은 `projectb-verify` §1. 판정 = `TEST_OK` + exit 0 + `SCRIPT ERROR` 없음.
- 🔴 **같은 집 두 PC 테스트 = `bash scripts/dev_local.sh`** (로컬 릴레이 :9080 + 웹 :8910, LAN IP 자동 감지·안내 출력). 공용 릴레이는 한국→홍콩→한국을 왕복해 **RTT 140~215ms**인데 로컬은 **13.8ms**다(실측 2026-07-24) — 개발 중 반복 테스트는 반드시 이걸로. 클라가 페이지 호스트를 보고 릴레이를 자동 판별하므로 `?relay=` 부착 불필요(`Net.default_relay_url`). `--fast`(익스포트 생략) · `--stop`(Ctrl+C가 안 먹었을 때 정리). ⚠ 호스트도 **LAN 주소**로 열어야 초대 링크가 공유 가능하다(localhost로 열면 링크에 localhost가 박힌다). 배포본(`game.jachana.com`)은 심사위원·원격 친구용으로 그대로.
- **지연 진단(스위트 아님):** `tests/measure_latency.gd` — 릴레이 왕복 RTT + 회피 창 예산 환산. "렉이 있다·게스트만 맞는다" 신고 시 추측 전에 먼저 잰다. 실행법·해석은 `projectb-verify` §1.
- 중계 서버 로컬 실행: `./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://server/relay/relay_server.gd -- --port=9080`
- **실배포(탈PC, 로컬 프로세스 0개):** 릴레이 = Cloudflare Workers(`server/relay-worker`, `npx wrangler deploy`), 웹 = 정적 서빙 Worker(`server/game-worker`) — **웹 배포 정본 = `bash scripts/deploy_web.sh`**(익스포트→wasm gzip 스테이징→deploy). 주소 = `https://game.jachana.com`(?host / ?join=코드) + `wss://relay.jachana.com`. 상세·함정은 `projectb-rules` §2 server. (구 `scripts/start_multi.bat` 터널 방식은 로컬 개발 폴백)
- **웹 익스포트:** `./Godot_v4.7.1-stable_win64.exe --headless --path . --export-release "Web" build/web/index.html` → `cd build/web && python -m http.server 8910` 후 브라우저에서 `http://localhost:8910` 확인.
  - 웹 템플릿 필요: `%APPDATA%/Godot/export_templates/4.7.1.stable/` (web_*.zip — 설치돼 있음).
  - `export_presets.cfg`는 **커밋돼 있다**(웹 프리셋뿐, 비밀값 없음 — 클론만으로 익스포트 가능). `thread_support=false` 필수, `exclude_filter`에 `.mcp.json, .claude/*, docs/*, memory/*`. 모바일 프리셋(서명 비밀번호)을 추가하게 되면 그때 다시 gitignore로 분리.

**협업 배포 (2인 체제 — "배포해줘" 한마디로 끝나야 한다):**
- **djgnfj** (이 머신) = jachana.com 도메인·Cloudflare 계정 소유자. **b-hy\*** (친구) = 도메인 없음 → workers.dev 임시배포.
- **양쪽 다 같은 명령이다: `bash scripts/deploy_web.sh`.** 스크립트가 `wrangler whoami` 계정을 보고 자동 분기 — jachana 계정이면 `wrangler.jachana.jsonc`(고정 주소 `game.jachana.com`), 그 외 계정이면 기본 `wrangler.jsonc`(routes 없음 → 자기 `*.workers.dev` 임시 주소).
- ⚠ djgnfj 머신에서 game-worker를 **수동으로** deploy할 땐 반드시 `--config wrangler.jachana.jsonc` — 기본 jsonc엔 routes가 없다. 두 jsonc는 routes만 다르고 나머지는 미러 — 설정을 바꾸면 둘 다 고친다.
- 친구 사전 준비(1회): Godot 4.7.1을 레포 루트에 다운로드 + 웹 익스포트 템플릿 설치 + `npx wrangler login`(무료 계정). `export_presets.cfg`는 커밋돼 있어 클론만으로 익스포트 가능.
- 릴레이는 친구가 배포하지 않는다 — 공용 `wss://relay.jachana.com`이 클라이언트 기본값(`Net.DEFAULT_RELAY_URL`)이라 workers.dev 임시배포·네이티브 클론 실행 모두 `?relay=` 없이 바로 붙는다. (릴레이 배포 = djgnfj 전용, `server/relay-worker`.)
- djgnfj는 친구가 보낸 workers.dev 링크(`…?host`)를 브라우저로 열기만 하면 됨 — 로컬 설치·실행 불필요. 심사위원은 README.md의 "바로 플레이/클론해서 실행" 참조.
- ✅ workers.dev 분기 실기 검증 완료 (2026-07-23, b-hy 첫 배포 성공 → `https://projectb-game.youqlrqod.workers.dev`). 이때 발견한 함정 2건을 해소함: 웹 익스포트 템플릿 미설치(→ 설치 완료), `build/web` 폴더 부재로 익스포트 실패(→ deploy_web.sh에 `mkdir -p` 추가).

**커밋 규약:**
- **메시지는 한국어로 쓴다.** (요약 줄 + 필요 시 본문. 트레일러 `Co-Authored-By`·`Claude-Session`은 형식이라 그대로 영어로 붙인다.)
- **요약 줄은 `동사: 내용` 형식.** 접두 동사는 아래 목록에서만 고른다:

  | 접두 | 쓰임 |
  |---|---|
  | `추가:` | 새 기능·파일·에이전트·스킬 |
  | `변경:` | 기존 동작·기획을 바꿈 (예: `변경: 성장 방식을 레벨업 → 장비 강화로`) |
  | `수정:` | 버그 고침 |
  | `삭제:` | 제거 |
  | `문서:` | 문서·주석만 손댐 |
  | `정리:` | 리팩터·포맷 (동작 변화 없음) |

- **한 커밋 = 한 논리 변경.** 성격이 다른 변경(예: 에이전트 추가 + 버그 수정 + 문서 정리)이 워킹 트리에 섞여 있으면 **한 번에 커밋하지 말고 먼저 물어라** — 어떻게 쪼갤지 사용자에게 확인받은 뒤 나눠 커밋한다. `git add -A`로 전부 쓸어담기 전에 `git status`로 무엇이 섞였는지 본다.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-07-21 | 탁본 하네스를 Project_B용으로 이식 (에이전트 9종 `takbon-*`→`projectb-*`, 스킬 `takbon-rules/verify`→`projectb-rules/verify` 재작성, Godot 4.6.1→4.7.1) | 전체 | 탁본 `.claude` 복사본을 신규 2D 프로젝트에 맞춰 일반화. 게임 고유 사실은 걷어내고 보편 규율만 유지 |
| 2026-07-21 | 기획 담당 `projectb-planner` + 진실원 문서 `docs/GDD.md` 신설, GDD 수정 승인 게이트(settings.json `permissions.ask`) 추가 | agents/projectb-planner.md · docs/GDD.md · .claude/settings.json | 하나의 진실원 기획 문서로 개발하고, 그 문서 수정은 사용자 승인을 받도록 |
| 2026-07-21 | 프로젝트 정체를 웹 게임(Web/HTML5 익스포트)으로 확정, 네트워크 리뷰 상시 필수 규칙 추가 | CLAUDE.md · projectb-rules · projectb-reviewer | 웹 게임이라 네트워크가 코어 — 리뷰에서 WebSocket·권한·검증을 매번 확인 |
| 2026-07-21 | "비주얼은 스프라이트 기본, 도형으로 때우지 마라" 규약 추가 | projectb-rules §0 · projectb-dev · projectb-reviewer | 임시라도 스프라이트(텍스처)로 만들어야 나중에 이미지 교체가 쉬움 |
| 2026-07-21 | 기획 검증 에이전트 `projectb-critic` + `projectb-plancheck` 스킬(7 정합성 렌즈) 신설 | agents/projectb-critic.md · skills/projectb-plancheck | planner가 만든 기획을 적대적으로 검증(생성-검증 짝). 모순·안 닫히는 루프·비현실적 범위를 잡되 GDD는 안 고침 — 수정은 planner 승인제로 |
| 2026-07-21 | 커밋 규약 신설 — 한국어 메시지, 요약 줄 `동사: 내용` 형식(추가/변경/수정/삭제/문서/정리), 한 커밋=한 논리 변경(섞이면 먼저 질문) | CLAUDE.md 커밋 규약 절 | 커밋 언어·형식·단위를 고정해 이력을 읽기 쉽게, 뒤섞인 커밋 방지 |
| 2026-07-21 | `projectb-rules` §1~§5를 GDD 기준 실제 값으로 채움 — 오토로드 6종(EventBus·Net·GameState·Db·SaveManager·Audio)+호스트 권한 모델, 모듈 지도 10종, 예약 하드 계약(combat_math·net_schema·호스트 확정), 데이터 주도 매핑, 물리 레이어 배정표(7층)+웹·멀티 고유 함정 | projectb-rules · CLAUDE.md 상태 줄 | GDD v1.4 확정에 따라 구현 착수 전 아키텍처 지도를 고정 — 이후 모든 위임이 같은 지도를 봄 |
| 2026-07-21 | Godot 프로젝트 뼈대 생성(project.godot — Compatibility·Nearest·640×360 임시+정수배, 부팅 씬 src/main) + 4.7.1 웹 템플릿 설치 + 웹 익스포트 파이프라인 검증(빌드 산출·로컬 서버 200 확인). 웹 익스포트 정본 명령을 검증 명령 절에 추가 | project.godot · src/main · CLAUDE.md | 웹 빌드가 제출물의 전부 — 익스포트 리스크를 Day 1에 제거 |
| 2026-07-21 | 멀티 골격 스파이크 — 자체 JSON/WebSocket 프로토콜(net_schema 단일 소스) + 중계 서버(server/relay, 방 코드 스코프 릴레이) + Net 오토로드 + 로비/테스트 스테이지(피어당 스폰·위치 동기화). 첫 자동 테스트(tests/test_net_room_auto.gd — 방 왕복 + 방 종료 후 재생성, 뮤테이션 2종으로 검출력 증명), reviewer 네트워크 리뷰 Critical(방 종료 후 상태기계 데드락) 수정. 함정 추가: `-s`에선 오토로드 전역 식별자 컴파일 불가 → core/net은 /root+class_name 접근 | src/core·src/net·server/relay·src/player·src/stage·src/ui·tests · projectb-rules §2·§3·§5 · projectb-verify §1 | 네트워크가 코어(웹 게임) — 멀티 구조를 먼저 뚫고 그 위에 게임플레이를 쌓기 위해 |
| 2026-07-21 | 전사 슬라이스 1단계 — 마우스 조준(2방향 플립)·좌클릭 공격(_unhandled_input, 원형 질의)·Shift 구르기 + 허수아비 적(EnemyDef·dummy.tres). 전투 동기화 = 호스트 권한: hit_req 사거리+쿨다운 검증(CombatMath, 같은 스윙 다중 타격 허용), 부활 확정 호스트 전용(enemy_hp_confirmed→ehp 브로드캐스트), ehp 송신자 검증, 원격 변위 클램프(순간이동 스푸핑 완화), 공격자 job 기반 판정. 테스트 추가: test_combat_math_auto(경계값, 뮤테이션 검출 확인) | src/core(combat_math·enemy_def·event_bus·net_schema)·src/enemies·src/player·src/stage·data·tests·project.godot | GDD v1.5 조작 확정 → 전투 코어 착수. reviewer Critical 3·Important 2 반영 |
| 2026-07-21 | 브라우저 실기 검증 정비 — 한글 픽셀 폰트(Galmuri9, OFL) 임베드(한글 전부 tofu였음), 로비 중앙 레이아웃 수정(비-Control 부모에서 앵커 미적용 → 뷰포트 크기 강제), 방 코드 HUD(스테이지 좌상단), 자동 시작(`?host`/`?join=코드` — 초대 링크 스트레치의 골격, 네이티브 `--host/--join=`), 웹 디버그 입력 브리지(`?debug=1`에서만 `window.pb_press/pb_release`). 크롬 2탭 실기 검증 통과: 참가·위치/공격 연출 동기화·게스트 공격→호스트 확정→사망→부활 브로드캐스트 수렴. 함정 등록: 크롬 백그라운드 탭 프리즈 = 호스트 정지 | assets/fonts·src/ui/lobby·src/stage·src/core/debug_bridge·project.godot · projectb-rules §5 | 웹 실기에서만 드러나는 결함(폰트·레이아웃·검증 불가능성)을 Day 1에 제거 |
| 2026-07-22 | 서버 탈PC 배포 — 릴레이를 Cloudflare Workers+Durable Object로 포팅(`server/relay-worker`, relay.gd와 1:1 + 배포 필수 3종: 연결 64·방 24·메시지 2KB 상한, 60초 스윕·3분 좀비 정리), 웹 빌드 정적 서빙 Worker(`server/game-worker` — wasm 25MiB 제한을 gzip 사전압축으로 우회), 배포 정본 `scripts/deploy_web.sh`. 프로토콜 동일성은 기존 `test_net_room_auto`를 `url=`로 겨눠 검증(로컬 wrangler dev·실배포 wss 둘 다 TEST_OK). net_schema에 FAIL_SERVER_FULL 추가. 함정: wrangler dev는 127.0.0.1만(localhost는 IPv6), R2는 대시보드 활성화 필요라 미사용, Pages는 파일 25MiB 제한으로 포기 | server/relay-worker·server/game-worker·scripts·src/core/net_schema.gd · projectb-rules §2 · CLAUDE.md | "프로세스가 왜 이렇게 많냐" — 로컬 프로세스 0개 목표(메모리 과제 ②), 링크 공유만으로 상시 접속 |
| 2026-07-22 | 초대 원클릭 UX — 스테이지 HUD "초대 링크 복사" 버튼(`?join=코드` 전체 URL 클립보드, 비-기본 릴레이만 `&relay=` 부착), 초대 URL·릴레이 기본값을 `Net`(`invite_url()`·`default_relay_url()`) 단일 소스로 통합, 로비 단순화(방 만들기 큰 버튼 + 코드 참가 폴백, 서버 주소는 접힌 고급 옵션). 실기 검증: 도메인 멀티 정상. 리뷰 Critical 0·Minor 반영(focus_mode 0·피드백 타이머 중첩·네이티브 relay 중복) | src/net·src/ui/lobby·src/stage | 심사위원/친구가 링크 클릭만으로 판까지 도달해야 함(메모리 과제 ①, GDD §10) — 주소·코드 수동 복사 불만 제거 |
| 2026-07-22 | 협업 배포 절(2인 체제) 신설 — djgnfj = jachana.com 소유·고정 주소 배포 담당, 친구(b-hy\*) = 도메인 없음 → workers.dev 임시배포(routes 삭제 + 공용 relay.jachana.com 재사용 + `?relay=` 링크 부착) | CLAUDE.md 협업 배포 절 | 친구가 코드를 고쳐 임시배포하면 djgnfj가 링크만 열어 확인하는 협업 흐름을 문서화 |
| 2026-07-22 | 클론-앤-배포 원커맨드화 — `export_presets.cfg` 커밋(비밀값 없음), game-worker wrangler를 기본(routes 없음, workers.dev)/`wrangler.jachana.jsonc`(고정 주소)로 분리, `deploy_web.sh`가 `wrangler whoami` 계정 보고 자동 분기 + Godot 실행 파일·템플릿·로그인 사전 점검, 릴레이 기본값을 공용 `wss://relay.jachana.com`으로(웹 localhost 페이지만 로컬 릴레이), README를 심사위원용 "바로 플레이/클론 실행/배포" 안내로 재작성 | .gitignore·export_presets.cfg·server/game-worker·scripts/deploy_web.sh·src/net/net.gd·README.md·CLAUDE.md | 친구(b-hy\*)·심사위원이 클론 후 "배포해줘" 한마디로 임시배포·플레이까지 되게 — 수동 단계(프리셋 전달·routes 삭제·`?relay=` 부착) 제거 |
| 2026-07-22 | 전체 구조 리뷰 반영을 하네스에 고정 — projectb-rules §3에 히트 기하 하드 계약(attack_center_offset·attack_radius — 판정과 FX가 같은 함수) 추가, §2에 "예정된 리팩터 게이트" 신설(잔몹 착수 전 enemy HP·피격 컴포넌트 분리 · 두 번째 씬 착수 전 스테이지 동기화/전투 확정 공용화 · 권한·동기화 로직 복사 금지 원칙). 코드 반영은 e2825e4(재접속 스폰 화면 밖 이탈 수정 + 기하 단일 소스화 + 비트마스크 상수화) | projectb-rules §2·§3 · CLAUDE.md | 리뷰가 찾은 "다음 작업에서 갈라질 이음새"를 일회성 지적으로 흘리지 않고 규칙으로 고정 — 이후 모든 위임·리뷰가 착수 전 게이트로 보게 (사용자 요청: 하네스에서 항상 신경) |
| 2026-07-22 | 직업 선택 + 캐릭터 스프라이트 3종 — 로비 직업 선택 UI(3버튼·기본 전사, 초대 링크 자동 시작은 직업 클릭 후로 연기), 사용자 제공 aseprite 3종(warrior/archer/mage_16, 버전 접미사 제거) 임베드, GameState 오토로드 신설(선택 보관 + data/jobs 스캔 allowlist 리졸버 — 모르는/조작 id는 기본 직업 폴백), JobDef에 id·sprite, G_JOB 직업 공지(스테이지 입장 + 피어 합류 + 상대 첫 G_POS 시 재공지). 리뷰 Important 3 반영: 공지 유실 경합(첫 G_POS 재공지)·판 도중 직업 변경 차단(피어별 첫 공지 잠금)·직업 목록 하드코딩 제거(폴더 스캔). 테스트 추가: test_game_state_auto(리졸버 신뢰 경계, 뮤테이션 검출 확인) | src/core(game_state·job_def·net_schema)·src/player·src/stage·src/ui/lobby·data/jobs·assets(sprites/player·aseprite)·tests·project.godot · projectb-verify §1 · CLAUDE.md | 직업 선택→그 캐릭터로 플레이 (온보딩 첫 단계, GDD §7). 직업이 호스트 검증(사거리·쿨다운) 기준이라 네트워크 공지·잠금·allowlist 필수 |
| 2026-07-22 | 마을 씬 + 씬 전환 골격 — 리팩터 게이트(두 번째 씬) 통과: 피어/직업 동기화를 `src/net/peer_sync.gd`, 전투 확정을 `src/stage/combat_authority.gd`, 방코드/초대 HUD를 `src/hud/stage_hud`로 추출(마을·스테이지가 자식 노드로 공유). G_POS에 씬 id 부착 + 원격 스폰을 "첫 G_POS(같은 씬 증명)"로 일원화(다른 씬 피어 유령 스폰 방지), G_SCENE(호스트 → 전원 씬 전환 지시, HOST_ID 발신만 신뢰 + 씬 id allowlist) 신설. 흐름 = 로비 → 마을(잔디 타일·돌 아치 게이트, interact 레이어 7) → 호스트가 게이트 앞에서 **F(`interact` 액션, 키 변경 용이)** 로 출발 확인 시 전원 스테이지 돌입(게이트 안 안내: 호스트 "F — 출발"·게스트 "방장이 출발"). HUD 초대 버튼은 URL이 아니라 **방 코드 복사**(사용자 확정 — 환경 따라 URL이 안 통해 코드 입력 흐름이 확실). 함정 등록: 자식 컴포넌트 `_ready`에서 부모 add_child는 busy로 조용히 실패 → `call_deferred`(웹 실기에서 발견·수정). 검증: 헤드리스 3종 TEST_OK + 마을 실기(사용자, 밟으면-출발 판) — F 상호작용판 실기는 인계 | src/net/peer_sync.gd·src/stage(combat_authority·test_stage)·src/hud·src/village·src/main·src/core(net_schema·event_bus)·src/player·project.godot(interact)·assets/sprites/village·tests 무변경 · projectb-rules §2·§5 | GDD §3 마을 거점 착수 — 두 번째 씬이 생기기 전에 권한·동기화 공용화(게이트), 이후 챕터 스테이지도 같은 컴포넌트를 문다 |
| 2026-07-22 | 잔몹 + 플레이어 피격/데스 — 전투 루프 완성. 잔몹 게이트 통과: HP·피격·부활을 `src/combat/health_component.gd`(권한/표시 경로 분리)로, G_SCENE 송수신을 `src/net/scene_flow.gd`로 추출. 구르기 i-frame(GDD v1.6 확정) = CombatMath 단일 소스(상수 이사+그랜트/창 검증), 잔몹 chaser(호스트 전용 FSM·텔레그래프 기믹·mpos 10Hz 배치), 데스 룰(관전 고스트→클리어 HP1 부활·전멸→마을 귀환 — 전부 호스트 확정), HUD HP/배너, pb_hurt 계측. 리뷰 Critical 1(사망자 hit_req 거부)·Important 1(freed self 람다) 반영. 게이트 신규 2건 예약(동적 스폰 등록·재합류 스냅샷), §3 계약 2건 등록(구르기 타이밍·잔몹 타격 기하), 테스트 스위트 4종으로 확장 | src/combat·src/enemies·src/stage·src/net·src/player·src/hud·src/village·src/core·server·data·assets·tests · projectb-rules §2·§3 · projectb-verify §1 · docs/GDD.md v1.6 | 판을 성립시키는 위협·죽음·클리어가 없었음 — 챕터1(스테이지→보스)로 가는 전투 코어 완성 |
| 2026-07-22 | 플레이어 32px 리드로우 + idle/run/roll 애니 — 스프라이트 3종 16→32px(세피리아풍, 얼굴 가림: 전사 그레이트헬름·궁수 후드 그늘·마법사 챙모자 그늘, 사용자 방향 확정), 각 10프레임(idle 2·run 4·roll 4=ROLL_TIME_S 정합) 시트+SpriteFrames, JobDef.frames 신설·플레이어 AnimatedSprite2D 전환(상태머신 roll>run>idle), G_ROLL 원격 연출 소비(peer_sync, 표시 전용·스팸 잠금 차단), 로비 아이콘 JobDef.sprite 데이터 바인딩(16px 하드코딩·placeholder 제거). 리뷰 Critical 0·Important 1(로비)·Minor 3 반영, 헤드리스 4종 그린 + 로컬 웹 실기 확인(사용자). §3에 ROLL_TIME_S↔roll 애니 미러 계약 등록 | assets(aseprite·sprites/player)·data/jobs·src/core(job_def·combat_math·net_schema)·src/player·src/net/peer_sync·src/ui/lobby · projectb-rules §3 | 32px 확정(몬스터 32/48 대비 동급 비율·예비동작 가독성) 후 첫 애니 — 이후 프레임 교체만으로 개선 가능한 베이스 |
| 2026-07-22 | 전사 무기 독립 스프라이트화 + 무기 규격 확립 — 몸에 굽는 방식 폐기(장비 교체 불가), 무기 = WeaponPivot/Weapon + JobDef.weapon_texture/grip 데이터 바인딩, 대검(조준 회전·3박자 스윙·스워시 궤적 FX·판정-시각 정합 range 42). 규격을 하네스에 고정: projectb-art(우향 수평·그립 좌표 보고·상하 대칭), rules §4(새 무기 = 텍스처+값 2개+range 정합, 공용 경로 자동 처리 목록), §2 게이트(비-스윙 모션 착수 전 모션 타입 데이터화), §3(스윙 창 미러·Inf 주입 가드) | src/player·src/net/peer_sync·src/core(job_def·net_schema)·data/jobs·assets(weapons·fx·aseprite)·projectb-art·projectb-rules §2~§4 | 사용자 방향 확정: "무기별로 반드시 이 구성" — 캐릭터별 장비를 만들 수 있는 구조 + 이후 모든 무기가 같은 규격을 따르게 |
| 2026-07-22 | 챕터1 스테이지 진행 골격 — 챕터 = 데이터(`data/chapters/chapter1.tres`, ChapterDef 칸 목록: 전투3+모닥불2, 마지막=보스 자리 브루트), G_SCENE에 챕터 좌표(c/i) 부착(검증 단일 소스 = GameState.is_valid_stage — allowlist+범위, 씬 경로는 로컬 리졸브만), 진행 결정 = chapter_flow(호스트: 클리어→다음 칸·완주/전멸→마을), 모닥불 씬(앉기 슬로우 회복 = 호스트 confirm_hp, G_SIT은 힌트·거리 재검증), **챕터 내 스테이지 간 HP 이월**(record/carried_party_hp + player_spawned 재확정 + set_max_hp로 직업 재공지 리셋 방지), 칸 토큰("stage:챕터:인덱스")으로 재사용 씬 유령 스폰 방지, HUD 진행 라벨·챕터 클리어 배너. 함정 2건 등록(씬 스왑 프레임 유령 어그로 → CHASE 리시, 바닥 z=-10). 아트 3종(사냥터 바닥·모닥불·브루트 48px). 검증: 헤드리스 4종+뮤테이션 2종, 웹 솔로 실기 전 루프(마을→1→모닥불→2→모닥불→보스→완주 귀환, 사망→클리어 부활 포함) — 이후 플레이 테스트는 사용자 몫(솔로 포함, 리드는 화면 확인까지) | src/core(chapter_def·campfire_def·game_state·net_schema·event_bus·enemy_def·debug_bridge)·src/stage(stage_1/2/boss·campfire·chapter_flow·stage)·src/net·src/player·src/combat·src/enemies·src/hud·src/village·src/main·data(chapters·campfire·enemies)·assets·tests · projectb-rules §2~§5 · projectb-verify §1 | 확정 범위(GDD §10) 병목 = 챕터1 구조 — 보스·드랍·모닥불이 전부 이 위에 얹힌다 |
| 2026-07-22 | jachana.com 고정 배포 + 실기 진단 계측 + 사거리 검증 좌표 수정 — Cloudflare named tunnel `projectb`(기존 ideaforge와 별개, quick tunnel이 기본 config에 가로채이는 함정 해결)로 `game/relay.jachana.com` 고정 주소, 로비가 `game.*` 호스트에서 릴레이 기본값 자동 설정, `?relay=` 초대 링크 파라미터, `scripts/start_multi.bat` 일괄 시작. 계측: 릴레이 비-pos 이벤트 로그, 브리지 pb_dump + InputEventAction 주입. **수정: 호스트 사거리 검증이 보간 표시 좌표를 써서 랙 시 정당한 적중을 거부 → net_anchor()(클램프된 최신 수신 좌표)로 변경.** 도메인 경유 전 구간 실기 검증: 참가→이동 동기화→게스트 공격→호스트 검증·확정(30→20)→게스트 반영 | server/relay·src/core/debug_bridge·src/player·src/stage·src/ui/lobby·scripts · projectb-rules §2·§5 · CLAUDE.md | 원격 친구와 바로 플레이 가능한 고정 주소 확보 + 랙 환경 오탐 제거 |
| 2026-07-23 | 손맛 연출 계층(`src/feel`) + 효과음 시스템 — 표시 전용 손맛을 EventBus 훅(combat_impact·screen_shake·player_swing/roll·entity_died)에 얹어 **네트워크 메시지 0개**(호스트/게스트 각 클라 로컬 재생). 로컬 추적 카메라+스크린셰이크·맞은 대상만 히트스톱(time_scale 금지)·히트플래시 셰이더(번쩍 후 머티리얼 제거로 웹 잔상 차단)·데미지 숫자 팝(DamageNumbers 오토로드)·피격 플린치·그림자·먼지. Audio 오토로드(SFX 8채널 + 마스터 볼륨/뮤트 `user://settings.cfg`, SFX 6종 절차 생성 `scripts/gen_sfx.py`) + 소리 설정 UI(로비·HUD). 웹 실기 검증(사용자): 타격감·색 복귀·소리 확인. 커밋 3분할(연출+오디오 코어 / 설정 UI / 하네스 문서) 후 master 머지 | src/feel·src/core(audio·event_bus)·src/player·src/enemies·src/ui·src/hud·assets(shaders·audio·sprites/fx)·scripts·project.godot · projectb-rules §1·§2 | GDD §5 전투에 "손맛"(히트스톱·플래시·셰이크·소리)이 없어 타격이 밋밋 — 아트/손맛 축을 세움 |
| 2026-07-23 | I키 인벤 창 + HUD 골드 우하단 재배치 (`feat/inventory-ui`) — HUD 골드를 **우하단** 아이콘+숫자(금색)·HP 바 좌하단 유지·재료 HUD 제거→I인벤. 신규 `src/ui/inventory_panel`(craft_panel 모달 패턴 복제 — 재료·장비 조회 + 장비 장착, 어디서나 I 토글·Esc 자체 닫기·**HUD가 I키 소비**(패널은 I 안 먹음, 중복 방지)·게임 안 멈춤·마우스만 모달). input map `inventory`(I) 신설. 제작/강화는 마을 제작대(F) 그대로. 사용자 제공 탑다운 HUD 레퍼런스(재화 우하단) 기반. projectb-ui 위임. ⚠ mouse_filter·클릭 도달·렌더 실기 확인 몫(verify §2-1) | project.godot·src/hud/stage_hud·src/ui/inventory_panel(신규) | HUD 간결화(돈만) + 재료/장비 조회·장착 동선 (사용자 요청) |
| 2026-07-23 | 드랍 손맛 실기 조정 + HUD 인벤/HP (슬라이스 4 후속, `feat/loot-crafting`) — **자석 픽업**(로컬 플레이어 끌림 반경 36px 안 아이템을 흡입, 가까울수록 가속·각 클라 자기 플레이어 기준·네트워크 0, 선착 확정은 호스트 그대로 rules §3)·**등장 토스 아크**(위 15px→bounce 착지, SETTLE 0.45s 동안 자석/bobbing 억제 = 튀어오르는 등장 후 흡수, "바로 빨림" 해소)·**HUD 인벤 좌하단 아이콘+숫자**(골드·재료 종류별, GOLD_TEX·MaterialDef.icon, mouse_filter IGNORE)·**HP 바 빨강**(StyleBoxFlat fill + 라벨 아웃라인). 전부 표시/로컬. 값은 연출 const(실기 튜닝). 웹 익스포트 컴파일 게이트 통과·game.jachana.com 배포 | src/stage/drop_item·src/hud/stage_hud(.gd·.tscn) | 사용자 실기 요청 — 픽업/등장 손맛·인벤 가독 |
| 2026-07-23 | 드랍 슬라이스 4 — 드랍 등장/픽업 손맛 + SFX (브랜치 `feat/loot-crafting`). **전부 표시 전용·네트워크 0** — 기존 EventBus 훅(`item_dropped`·`item_picked`·`blueprint_unlocked`)에 구독자만 얹음(판정 코드 무접촉). **시각**: `drop_item` 등장 스케일 팝(0.2→오버슛→1 트윈)·rarity 1/2 등급 반짝임(modulate 펄스, bobbing과 프로퍼티 분리) + 신규 오토로드 `src/feel/drop_fx`(DamageNumbers 미러 — item_dropped=아래 먼지·item_picked=위 반짝 CPUParticles 버스트, gold/등급 색, one_shot 타이머 정리·누수 없음). **청각**: SFX 4종(drop·pickup_item·pickup_gold·blueprint 팡파레) `gen_sfx.py` 절차 생성·임포트, Audio 훅 구독(도면은 팡파레가 픽업음 대신 — 중복 방지, picker만 1회). 발화는 전부 호스트 확정 픽업(drop_spawn_local/drop_pick_local + HOST_ID 검증) 뒤 → 각 클라 1회. reviewer Critical/Important 0·Minor 3(파티클 emitting 순서 반영·도면 비-picker 무음은 개인보상 정책 유지·씬전환 잔상 방치). 헤드리스 5종 그린(회귀 0). ⚠ 파티클/반짝임 렌더·SFX 소리·2클라 픽업음 1회 실기는 사용자 몫 | src/feel/drop_fx(신규)·src/stage/drop_item·src/core/audio·scripts/gen_sfx.py·project.godot(DropFx 오토로드)·assets/audio/sfx(4종) · projectb-rules §1·§2 | GDD §5 — 수집(드랍→줍기)에 등장/획득 손맛이 없어 밋밋 → 손맛 축 완성 |
| 2026-07-23 | 드랍 슬라이스 3 — 도면 "설계도 획득!" 토스트 + 전멸 저장 롤백 검증 (브랜치 `feat/loot-crafting`). **보스 핵심재료는 슬라이스 2 데이터로 이미 확정**(brute_core `is_core`/rarity2 금색 + brute.tres 100% 드랍 + iron_greatsword 레시피 게이트 + stage_boss 배선) — 이번엔 코드 변경 없음. **도면 토스트**: HUD 독립 Toast 노드(`blueprint_unlocked` 구독 → 레시피명 표시·2.5s 타이머 소멸·상태 배너와 분리, `mouse_filter=2`, 표시 전용·네트워크 0). 발화 = 호스트 확정 픽업(`G_PICK_OK`) 뒤 주운 클라 로컬 인벤 변동만 → 권한 모델 정합. **전멸 롤백 검증**: 신규 `test_save_manager_auto`(commit→전멸 reload 롤백=클리어분 생존·전멸분 소실·무파일 첫 판 전멸 + 뮤테이션 검출). SaveManager 테스트 격리(`save_path`·`game_state_override` 주입 → 실제 `user://save.json` 무접촉, verify §1 세이브 경고 준수), `is_inside_tree()` 가드로 -s 로그 오염(get_node 에러) 제거. reviewer Critical/Important 0·Minor 2 반영(테스트 주석·토스트 폴백 raw id 노출). 헤드리스 5종 그린. ⚠ 도면 토스트 렌더·전멸→마을 롤백 웹 라운드트립 실기는 사용자 몫 | src/hud/stage_hud(.gd·.tscn)·src/core(save_manager·game_state)·tests/test_save_manager_auto · projectb-verify §1 · CLAUDE.md | GDD §6·§11 — 도면=레시피 언락 피드백(획득 감각) + 전멸 롤백(클리어분만 생존) 계약을 테스트로 고정 |
| 2026-07-23 | 드랍 슬라이스 2 — 마을 제작대 + 제작/강화 UI + 스탯 전투 반영 (브랜치 `feat/loot-crafting`). **장비 스탯 공지**: 각 피어가 `G_STATS`로 착용 총 스탯 공지(G_JOB 미러·스폰 전 버퍼링·재공지·데이터 유도 clamp) → 호스트가 `calc_damage`(공격 보너스)·`max_hp`(체력, `set_max_hp` 이월 보존) 확정에 반영. **제작대**(마을 F → `craft_panel` 모달: 제작/강화/장착 탭·인벤 바, ui)·**저장 모델**(클리어=commit·전멸=reload 롤백·거래 시 commit)·craft 빈슬롯 자동 장착·아트 제작대 스프라이트. reviewer 네트워크 리뷰 Critical 0·**Important 3 수정**(부풀린 hp clamp→`max_equip_stats` 데이터 상한·reload 무파일 emit·set_max_hp 하향 dropped=false). **테스트 확장**(combat_math 장비 스탯·game_state 제작/강화/저장/allowlist + 뮤테이션 검출 증명). 헤드리스 4종 그린. ⚠ 제작 루프·모닥불·웹 저장 라운드트립 실기는 사용자 몫 | src/net/peer_sync·src/player·src/stage/combat_authority·src/core(game_state·net_schema·save_manager)·src/combat/health_component·src/ui/craft_panel·src/village·tests·assets/sprites/village · projectb-rules §2·§3 · projectb-verify §1 | GDD §4·§6 — "죽고→강화→재도전" 루프가 코드상 닫힘 |
| 2026-07-23 | 드랍 슬라이스 1 — 등장·선착 픽업·멀티 동기화 (브랜치 `feat/loot-crafting`). **공유 드랍**: 호스트 롤 1회→`G_DROP` 브로드캐스트+로컬 스폰, 선착 픽업=`G_PICK_REQ`→호스트 확정→`G_PICK_OK`. 신설 `src/stage/drop_authority`(호스트 권한 롤·선착 확정)·`drop_field`(스폰/despawn·픽업)·`drop_item`(Area2D pickup 레이어6·로컬 플레이어 겹침만·bobbing)·combat_authority `enemy_killed` emit·HUD 골드/재료 카운트·전투 스테이지 3씬 배선. 아트 드랍 아이콘 5종(금화·가죽·엄니·핵심·도면 placeholder)+재료 `icon` 배선. **reviewer 네트워크 리뷰 Critical 1 수정**: 픽업 확정에 호스트 로컬 반영 누락(릴레이 에코 없음 → 호스트 화면 유령 잔류·인벤 미반영) → `EventBus.drop_pick_local` 시그널로 스폰 경로(`drop_spawn_local`)와 대칭화. Important(픽업 근접 검증)·Minor 4는 rules §2 게이트로 이월. 헤드리스 4종 그린(회귀 0). ⚠ 드랍/픽업 실기(오버랩·렌더)는 웹 2클라 확인 몫 | src/stage(drop_authority·drop_field·drop_item)·src/core/event_bus·src/hud·data/materials(icon)·assets/sprites/items · projectb-rules §2 | 수집(드랍→줍기)이 루프의 절반 — 슬라이스 2(마을 제작/강화)로 소비를 잇는다 |
| 2026-07-23 | 드랍→제작/강화 루프 착수 (브랜치 `feat/loot-crafting`) — GDD v1.7(개인별→**공유 드랍**·선착·호스트 권한, 드랍 4종=골드·일반/핵심 재료·**도면=레시피 언락 토큰**·장비는 제작으로만, 강화 골격=골드 비용·2슬롯·공/체 2스탯). **슬라이스 0(core 토대):** 스키마 5종(Material/Equip/Recipe/DropTable/DropEntry)·SaveManager 오토로드(commit/reload 롤백=전멸 시 클리어분만 생존)·GameState 인벤/제작/강화/리졸버(스캔 allowlist)·combat_math 장비 계약(total_stats·equip_stat_at_level·upgraded_stats·upgrade_cost + calc_damage 확장=미착용 항등 폴백)·net 드랍 메시지 3종(G_DROP·G_PICK_REQ·G_PICK_OK)·데이터(재료3·장비2·레시피2·고블린/브루트 드랍 테이블). 헤드리스 4종 그린(회귀 없음). ⚠ Db는 미구현(리졸버 GameState 흡수 — -s 호환). 다음=슬라이스 1(드랍 엔티티·선착 픽업 네트워킹, reviewer 네트워크 리뷰 필수) | docs/GDD.md · src/core(스키마·game_state·combat_math·event_bus·net_schema·save_manager)·data(materials·equipment·recipes·enemies)·project.godot · projectb-rules §1·§3 | 수렵액션 빌드업 축(드랍→마을 제작/강화)이 없어 "죽고→강화→재도전" 루프가 안 닫힘 — GDD §4·§6 |
| 2026-07-24 | 궁수 + 활 = 실제 이동 투사체 (브랜치 `feat/archer-weapon`) — **§2 두 번째 무기 모션 게이트 통과**: 공격 모션 타입을 `EquipDef.motion_type`(swing/shoot/thrust)로 빼고 `player.gd _do_attack` 타입 분기(`_swing_attack`/`_fire_arrow`). **투사체 = 결정론 직선**: 발사자 `G_SHOOT`(원점·방향·aid·사거리 r) 1회 → 각 클라 로컬 표시 화살 시뮬(위치 스트림 0), **명중은 호스트만**(`combat_authority._physics_process` 매프레임 `is_arrow_hit` 거리 질의 → `calc_damage` 확정 → `G_ARROW_HIT`). 신규 `src/combat/arrow`(표시 화살·충돌 없음)·`src/stage/arrow_field`(전 클라 관리). 무기별 `arrow_range`(사거리, 호스트 MAX clamp)·`weapon_hold_dist`(큰 무기 겹침 방지). 콘텐츠: 활/화살 도트(우향 규격, aseprite 소스)·`worn_bow.tres`·archer 배선·`bow_fire` SFX. **네트워크 리뷰 2회 Critical 0** — 신뢰 경계(사망자 거부·발사율·원점 근접·r clamp·aid 네임스페이스·dir/range 유한성)를 근접과 대칭으로. 헤드리스 5종 그린(combat_math에 화살 발사율/원점/명중/사거리 clamp 경계+뮤테이션)·웹 익스포트·game.jachana.com 배포. ⚠ 실기 튜닝 몫: SHOT_ORIGIN_TOL 44의 고속이동+고랙 정당발사 보존·활 사거리 220·연사 0.15·데미지 4. ⚠ bow.png는 아트 후 좌우 반전이라 aseprite 소스와 방향 상이 | src/core(equip_def·combat_math·net_schema·event_bus)·src/combat(arrow)·src/stage(arrow_field·combat_authority·스테이지 4씬)·src/player·src/net/peer_sync·src/core/audio·data(equipment/worn_bow·jobs/archer)·assets(weapons·audio·aseprite)·scripts/gen_sfx·tests·rules §2~§4·verify §1 | GDD §7 직업 = 플레이 방식의 변환(궁수 = 원거리). 전사 완성 후 궁수 스트레치 착수 |
| 2026-07-24 | 법사 + 지팡이 = 차지 범위 폭발 — **§2 세 번째 무기 모션 게이트 통과**: `EquipDef.motion_type="charge"`가 **shoot 경로를 재사용**(표시 탄·호스트 전진·G_SHOOT·G_ARROW_HIT 전부 공용) + 차이는 3곳(발사 입력 `_tick_charge`·종료 폭발 판정 `_confirm_blast`·폭발 FX `arrow_field._blast_fx`). **신규 네트워크 메시지 0개** — G_SHOOT에 `w`(무기 id, allowlist 리졸브)·`c`(차지 레벨) 필드, G_POS에 표시용 `c`(레벨+1 인코딩 = 원격 차지 오브). 사용자 확정: **3단계 차지**(0.35s/단계)·**위력+폭발 반경 동시 증가**·**차지 중 이동 50%**(구르기로 취소). §3 계약 2건 신설: 차지 계산(`CHARGE_*` 배율표·`is_charge_time_ok` 차지 시간 검증·`charge_blast_radius` MAX clamp·폭발 반경=blast.png 표시 반경 미러) + `GameState.projectile_params`(표시·판정이 같은 값을 얻는 리졸브 단일 소스). 콘텐츠: **첫 주문 = 속성 없는 기본 마력탄**(사용자 확정 — 초보 무기에 속성·과한 위력을 붙이지 않는다. 보라 아케인 → 파이어볼을 거쳐 무속성 은백으로 확정) 도트 3종(무색 수정 지팡이·마력탄·폭발)·`worn_staff.tres`·mage 배선·SFX 3종(charge_step·staff_fire·blast). 밸런스 하향: job 공격 14→9·장비 2/lv2→1/lv1·폭발 반경 28→20(0단계 10뎀/반경 20 → 3단계 34뎀/반경 48). **폭발 텍스처는 중립 그레이스케일 + `swing_color` 틴트**(현 무속성 = 은백 `(0.88,0.93,1)`, 이후 속성 지팡이는 색 한 줄로 불/얼음 — rules §4). 헤드리스 5종 그린 + **뮤테이션 2종으로 검출력 증명**(차지 시간 검증 제거→3 FAIL·폭발 반경 clamp 제거→1 FAIL) + 웹 익스포트·씬 로드 스모크. **reviewer 네트워크 리뷰 Critical 0 · Important 3 반영**: ⑴ 호스트가 G_SHOOT `w`를 안 믿고 `peer_sync.peer_weapon_id()`(G_STATS 공지 무기)로 리졸브 — 무기 사칭으로 차지 배율·광역을 얻던 표면 제거 ⑵ `GameState._equip_cache` — 발사마다 .tres 재파싱(샷당 2회+, 연사 시 ~13회/s)으로 생기던 WASM 히치 제거 ⑶ 고블린 `body_radius` 6→10(32px 스프라이트 정합, 브루트 48px=14와 비례). Minor 반영: 원격 차지음 스팸 게이트·`TEX_RADIUS` 실측 30.6(캔버스 32 아님 — 그린 원이 판정보다 4% 작던 것)·`player_swing` 재사용 경고 주석·`projectile_params` dead 키 제거. ⚠ 실기 몫: 차지 손맛(단계 시간 0.35·감속 0.5·폭발 반경 28/배율·데미지 14×3.4)·오브/폭발 렌더·2클라 원격 차지 표시 | src/core(equip_def·combat_math·game_state·net_schema·event_bus·audio)·src/combat(arrow·blast 신규)·src/stage(arrow_field·combat_authority)·src/player(player.gd·.tscn)·src/net/peer_sync·data(equipment/worn_staff·jobs/mage)·assets(weapons/staff·magic_orb·fx/blast·audio 3종·aseprite)·scripts/gen_sfx·tests·rules §2~§4·verify §1 | GDD §7 직업 = 플레이 방식의 변환(법사 = 모아서 터뜨리는 범위딜). 전사·궁수에 이어 3직업 완성 |
| 2026-07-24 | **지연 보상 — "피했는데 맞았다" 제거** (브랜치 `feat/lag-compensation`). 신고("게스트만 맞는다")를 계측으로 확인: 신설 `tests/measure_latency.gd`(게임과 같은 홉을 왕복, 스위트 아님)로 **배포 릴레이 RTT 140~215ms**(로컬 릴레이 14ms = 코드 오버헤드 하한). 원인 = 호스트 권한 모델의 **구조적 비대칭** — ⑴ 예고가 게스트 화면에 뜨기까지 편도 지연 ⑵ 호스트가 판정에 쓰는 게스트 좌표가 편도+송신주기만큼 과거 → 게스트 실효 회피 창 0.60s→0.38s(36% 손실), 호스트 0%. **해법 3종:** ⓐ `POS_SEND_RATE` 15→30Hz ⓑ **STRIKE 지연**(`strike_delay_s` — 예고 타격을 가장 느린 피어의 편도만큼 늦춰 모두가 온전한 예고 창을 갖게, 표시 지속도 같이 연장) ⓒ **위치 외삽 + 방어자 우대**(G_POS에 `vx/vy` → `net_anchor_lead`, **낡은 좌표와 추정 좌표가 둘 다** 맞아야 확정 → 외삽 오차가 항상 방어자에게 유리한 쪽으로만 떨어짐, 두 좌표가 같은 호스트/솔로는 항등). RTT 계측은 `Net`이 자체 처리(G_PING/G_PONG 0.5s EMA, `net_msg`로 안 올림) + HUD 방 코드 줄에 핑 표시. 신뢰 경계 = 편도 200ms·외삽 56px·속도 이동상한 clamp. **헤드리스 5종 그린 + 뮤테이션 3종 검출력 증명**(방어자 우대 제거→1 FAIL·외삽 상한 제거→1·편도 상한 제거→3) + 웹 익스포트. ⚠ **릴레이 DO `locationHint:"apac"` 재배치는 시도 후 롤백** — 같은 시간대 A/B(구 207/145/207 vs 신 214/–/152)에서 차이 없음, 실패 기록을 rules §5에 고정. ⚠ 함정 등록: Net이 소비하는 kind 이름이 기존 게임/테스트 kind와 충돌하면 조용히 깨진다(`"ping"`→`"nping"`). **+ 로컬 LAN 개발 서버 `scripts/dev_local.sh`**(사용자 요청 — "어차피 빠르게 테스트하는 게 중요"): 같은 집 두 PC가 홍콩을 안 거치게 로컬 릴레이+웹을 한 명령으로. `Net.default_relay_url`이 페이지 호스트가 사설 IP면 같은 호스트의 :9080을 쓰도록 확장(`_is_lan_host` — RFC1918, 172.16~31 경계 15케이스 검증) → 링크에 `?relay=` 불필요. **실측 RTT 200ms → 13.8ms**(회피 창 손실 36% → 5%, 나머지는 보상이 흡수). ⚠ 실기 몫: 2클라 회피 체감·핑 표시·예고 타이밍 | src/core(combat_math·net_schema)·src/net(net·peer_sync)·src/player·src/stage/combat_authority·src/enemies(mob_melee·boss_croc)·src/hud/stage_hud·server(relay·relay-worker)·tests(measure_latency 신규·combat_math 확장) · rules §2·§3·§5 · verify §1 | 사용자 신고 = "두 대로 하면 딜레이가 보이고, 나는 피했는데 맞았다는 피해자가 속출. 호스트는 괜찮은데 들어오는 사람만" — 협동 게임의 회피가 게스트에게만 불공정하면 보스전이 성립하지 않는다 |
| 2026-07-23 | 챕터1 보스전 (거대 악어 "늪의 군주") — 브랜치 `feat/boss-fight`. **슬라이스 0(core: `BossDef`/`BossPatternDef`·combat_math 부채꼴판정 `is_hit_in_cone`/솔로약화 `party_scale`/거대적 사거리 body_radius 반영·net 4종 `G_BOSS_ATK`/`G_SWAMP`/`G_BOSS_PHASE`/`G_BOSS_SPRAY`·EventBus 보스 시그널)·1(다패턴 호스트 FSM `boss_croc`·평타 부채꼴·mob_sync/combat_authority 배선)·2(★늪 기믹+슬램: `swamp_field`/`swamp_zone`·플레이어 로컬 슬로우·G_SWAMP 3중가드)·3(물뿌리기 N개 원 착탄·프레임 dedup)·4(페이즈2 자동늪+"페이즈2!" 배너+party_scale 솔로약화)** + 보스방 아레나(swamp_ground 타일+boss_arena 2층)·패턴 페이싱(`recover_s`/`priority`로 빈틈 제거·거리별 역할)·스프라이트 입체 리셰이드+접지 그림자. reviewer 네트워크 리뷰 3회 Critical 0, 헤드리스 5종 그린, game.jachana.com 배포. ⚠ **보스 아트 8방향 리디자인은 품질 이슈로 보류** — 커밋된 리셰이드 2방향 유지, 실험물 untracked 보존(나중 최적화 패스). 잠수 급습 패턴=컷 | src/core(boss_def·boss_pattern_def·combat_math·net_schema·event_bus)·src/enemies(boss_croc)·src/stage(swamp_field·swamp_zone·combat_authority·mob_sync·stage_boss)·src/hud·src/player·data/enemies(croc_boss)·assets(sprites/enemies·stage·fx)·rules §3·§4·verify §1 | GDD §2·§5 핵심 = 어려운 보스를 협동 학습으로 잡기. 챕터1 보스 3패턴+페이즈+늪 기믹 구현(패턴·수치 실기 튜닝은 이후) |
| 2026-07-25 | **기획 v1.8 — 성장 축 신설.** 직업 레벨 = 몹 킬 EXP(계열 공용 풀·파티 전원 동일 지급·전멸 시 재료와 같은 롤백) → 캐릭터 스탯 5종(치명타 확률/데미지·공격속도·이동속도·피흡) + 다음 하위 직업 해금. 직업 = 계열 구조(계열 고정·하위 직업 다수 보유·**메인 1개 + 서브 합산**·무기/모션 불변). 상한은 스탯별이 아니라 **총 화력 예산 ≤ 무장만 최대치의 1.5배**(공속·치명은 합계 데미지에 곱해져 개별 상한으로 안 묶임 — 전체 스프레드 4.5배). 축 경계 = 레벨은 공격력·체력 불가·장비는 5스탯 불가. planner 초안 → critic 검증(치명 4·경고 6) → 사용자 결정 5건 → GDD 반영. ⚠ `projectb-rules` 갱신은 **구현 착수 시**: §3에 치명타·피흡 계약(굴림은 호스트만·피흡 소수 누적)과 **공격속도 미러 계약**(검증 3함수 + 원격 표시 창 + 스탯 공지를 함께 고친다), §2 게이트에 **저장 스키마 버전 불변 규칙**(SAVE_VERSION 정확일치 검사 → 올리면 배포본 세이브가 조용히 전멸) | docs/GDD.md · CLAUDE.md 상태 줄 | 장비 강화(양) 축만으로는 성장 리듬이 얇고 "싸워서 강해지는" 감각이 없었음 — 질(質) 축을 얹어 분업 (사용자 요청: "캐릭터 스탯을 좀 추가할까 — 공속 이속 피흡 등등") |
| 2026-07-25 | **성장축 구현 (브랜치 `feat/growth-axis`, 슬라이스 0~5).** 0(core: `SubJobDef` 신규·`JobDef.starting_sub_job_id/exp_curve`·`EnemyDef.exp`·CombatMath 계약 8종·GameState 리졸버/계열 공용 풀/저장 2필드·net_schema 3종·EventBus 5종·`Health.last_crit`) · 1(EXP 루프 = `exp_authority` 호스트 확정 + HUD 레벨/EXP·토스트 큐) · 2~4(5스탯 전투 적용: G_STATS `lv` 공지·clamp · **데미지 확정 3경로를 `confirm_damage` 하나로 통합**(곱 순서·반올림 1회) · 치명 = 호스트 RNG·대상별 굴림 · 피흡 = 호스트 소수 누적 + `confirm_hp` 재사용 · 이속·공속 = `haste_scale`/`_move_speed` 단일 유도) · 5(마을 훈련소 = `subjob_panel`, craft_panel 규약 복제). **네트워크 신규 메시지 1개(G_EXP)뿐** — 나머지는 기존 메시지 필드 추가. 🔴 **reviewer 네트워크 리뷰 Critical 1 수정**: 호스트 자기 공속이 근접 게이트에 안 들어가 h>0.111부터 자기 타격이 한 번 걸러 무피해였다(`_peer_stats`엔 로컬 항목이 없다 — Net 루프백 없음) → 치명·피흡과 **소스 통일**. Important 3(외삽 상한이 이속을 못 덮어 지연 보상 퇴행 → 90px 재유도 + 이속 하드 상한 0.3 · 레벨 스탯 상한이 하위 직업 개수에 비례해 부풀음 → 정직 최대치로 좁힘 · 계열 혼용 시 HUD 표시 레벨이 거짓 → 곡선 계열 인자화)·Minor 2 반영. 헤드리스 5종 그린 + **뮤테이션 11종 검출력 증명** + 웹 익스포트. ⚠ 검증 함정 발견: 테스트의 딕셔너리 직접 인덱싱이 뮤테이션에서 SCRIPT ERROR를 내 **검출력 0을 통과로 위장**(verify §3) → `.get` 폴백으로 교정 + rules §5·verify 체크리스트 등록 | src/core(sub_job_def 신규·combat_math·game_state·net_schema·event_bus·job_def·enemy_def)·src/stage(exp_authority 신규·combat_authority·전투 3씬)·src/net/peer_sync·src/player/player·src/hud·src/ui/subjob_panel(신규)·src/village·src/feel(damage_numbers·camera_rig)·src/core/audio·data(subjobs 2장·jobs·enemies)·tests 3종 확장 · rules §2~§5 · verify §1 | GDD v1.8 — 장비(양) 축만으로는 성장 리듬이 얇고 "싸워서 강해지는" 감각이 없었음 |
| 2026-07-26 | **메인 전용 특성 축 + 검성 (GDD v1.9).** 하위 직업이 "5스탯 배분뿐"에서 "5스탯 + **메인일 때만 켜지는 특성 1개**"로 — 서브로 두면 특성이 꺼져 메인 선택이 계산기에서 **플레이 감각 선택**이 된다. 첫 특성 = 전사 계열 3번째 **검성**의 「검기 파형」(평타 사거리 **+30%**, 🔒 파형 자체 데미지 없음 → 곱해지지 않아 화력 예산 밖). **계약 = `CombatMath.effective_attack_range(job, reach)` 하나** — 사거리 3함수(`is_hit_in_reach`·`attack_center_offset`·`attack_radius`)가 전부 이걸 지나 "맞는 곳 = 보이는 곳"이 자동 보존된다(`haste_scale` 철학). **신규 네트워크 메시지 0개** — G_STATS에 `"ms"`(메인 하위 직업 **id만**) 필드 추가, 값은 각자 `data/subjobs`에서 리졸브(`GameState.reach_bonus_of` — 계열 불일치·모르는 id·경로 조작·상한 초과 폐기). 🔴 호스트는 사거리 보너스도 **공격자 아바타**에서 읽는다(공속·치명·피흡과 소스 통일 — `_peer_stats`엔 로컬 항목이 없어 검성 호스트가 자기 파형만 헛치는 2026-07-25 Critical의 재현을 선제 차단). 연출 = `player._start_wave/_tick_wave` + 신규 도트 `sword_wave.png`(중립 그레이스케일 → `swing_color` 틴트, 판정 기하에서 도달점 파생). **하위 직업 2→3개라 5스탯 스텝 전면 재역산**(검사·광전사 하향, 실측 총량 0.195/0.470/0.225/0.130/0.057 ≤ GDD 예산) + `test_game_state_auto`에 **예산 트립와이어** 신설(재역산을 잊으면 빨개진다). 헤드리스 5종 그린 + **뮤테이션 4종 검출력 증명**(상한 clamp 제거→4 FAIL·기하 직접 읽기→1·계열 검증 제거→1·재역산 누락→1) + 웹 익스포트. UI = 훈련소 패널 행에서 flavor 설명 제거(툴팁으로) + 특성 줄(켜짐 액센트/꺼짐 흐림)·패널 폭 492. ⚠ **`projectb-reviewer` 네트워크 리뷰 미실시**(세션 설정상 에이전트 위임 금지) — 리드 자체 점검만. ⚠ 남은 표면 = G_STATS `"ms"`의 **보유 검증 없음**(전사면 검성 미보유로도 사칭 가능, atk/hp와 같은 급 → rules §2 4인/PvP 게이트에 등록). ⚠ 실기 몫: 파형 렌더·사거리 체감·2클라 원격 파형·특성 줄 표시 | docs/GDD.md(v1.9)·src/core(sub_job_def·combat_math·game_state)·src/net/peer_sync·src/player(player.gd·.tscn)·src/stage/combat_authority·src/ui/subjob_panel·data/subjobs(검성 신규+2장 재역산)·assets(sprites/fx/sword_wave·aseprite)·tests 2종 확장 · rules §2~§4 · verify §1 | 사용자 요청 — "광전사랑 검성 만들어줘 / 검성은 평타 이후 파형이 조금 더 앞으로 감". 하위 직업이 스탯 숫자만 다르면 메인 전환이 계산기가 되어 GDD §5 "선택의 재미"가 얇았다 |
| 2026-07-26 | **기획 v2.0 — 하위 직업 조립 축 (구현 미착수).** ⑴ **장착 슬롯 = 메인 1 + 서브 2** — v1.8~v1.9의 "보유 서브 전부 자동 합산"을 대체. 안 낀 것도 EXP는 계속 적립(계열 공용 풀). 🔒 부수 효과 = 총 화력 예산이 **보유 개수가 아니라 슬롯 수**에 묶여 **"하위 직업 추가 = 전 계열 스텝 재역산" 부채가 사라진다**(검성 추가 때 실제로 치른 비용). ⑵ **특성이 자리별 두 얼굴** — 하위 직업 한 장이 메인 특성·서브 특성을 각각 정의하고 낀 자리에 따라 **하나만** 켜진다(v1.9 "하위 직업당 최대 1개" → **동시 활성 1개**로 정정, 판 전체 최대 3). 🔒 특성은 **곱해지지 않는 축만**(공격력·체력 = 장비 축 / 공속·치명 = 5스탯 축 → 둘 다 금지). ⑶ **공유 하위 직업**(계열 무관·🔒**서브 전용** — 메인은 항상 계열 것이어야 "계열 = 플레이 방식의 변환"이 유지) = **곡예사**(구르기 쿨 −15%)·**도굴꾼**(골드·재료 +15%), 해금 = **챕터1 보스 첫 처치에 2개 동시**(보유 5 > 슬롯 3이어야 조립이 시작). ⑷ **구르기 쿨 HUD**(안 보이면 −15%가 체감 안 됨). 🔴 **구르기 쿨 상한은 실제 상수로 검산해서 정했다** — i-frame 0.37s / 쿨 0.8s = **무적 시간 46%**가 이미 기본값이라, 상한 −40%면 **77%**로 사실상 상시 무적(§5 기믹 회피가 무너짐) → 상한 **−30%**(66%). 이 축은 작은 수치가 큰 효과를 내므로 다른 축보다 인색하게 잡는다. 특성 = **"효과 키 + 값" 카탈로그**(키 6종: 사거리 ✅기구현·구르기 쿨·구르기 거리·모닥불 회복·처치 후 이속·드랍량 — 키를 늘릴 때만 코드). 사용자 결정 4건(슬롯 2칸·공유는 서브 전용·포션은 보류·해금은 보스). ⚠ **구현 착수 시** `projectb-rules` 갱신 필요: §3에 **특성 키별 상한 clamp 계약**(사거리 `effective_attack_range` 패턴을 구르기 쿨·드랍량 등으로 확장 — 판정·표시가 같은 함수를 지나야 함) · §2 게이트에 **G_STATS 서브 id 2개 추가**(현 `"ms"` 미러 — 보유 검증 없음 표면이 3배로 넓어진다) · §4 "새 하위 직업 = 파일 한 장"에서 **재역산 딸림 문구 제거** | 사용자 요청 — "직업이 메인에 있을 때랑 서브에 있을 때 다른 효과를 갖게 해서 그걸 잘 조립하는 게 목표 / 서브는 공유되는 직업도 있을 듯". v1.9는 메인 1택뿐이라 조립할 것이 없었고, 서브는 보유만 하면 자동으로 붙어 선택이 아니었다 |
