# Project_B

**2D 픽셀아트 웹 게임** — Godot 4.7.1 · Web(HTML5/WASM) 익스포트 · 렌더러 Compatibility(웹 필수) · 엔진 실행 파일 = 루트의 `Godot_v4.7.1-stable_win64.exe`.

해커톤 마감 **2026-08-10**. 배포본 = `https://game.jachana.com`.

---

## 🔴 소유권 표 — 한 사실은 한 곳에만 산다

**이 표가 이 문서의 존재 이유다.** 같은 사실을 두 곳에 적으면 **반드시** 갈라지고, 갈라져도 에러가 안 나서 아무도 모른다(실제 고장 16건 — `docs/DECISIONS.md` 「하네스의 지배 고장 모드」).

| 무엇 | 정본 | 비고 |
|---|---|---|
| 게임이 **무엇인가** (비전·루프·메커니즘·콘텐츠) | `docs/GDD.md` | 🔒 **수정 시 사용자 승인 필수** (`.claude/settings.json`로 잠김). 절차 = `projectb-gdd` 스킬 |
| **어떻게 일하는가** (하네스·트리거·명령·규약) | **이 문서** | 스킬·에이전트와 충돌하면 여기가 이긴다 |
| 아키텍처 규칙·모듈 지도·하드 계약 | `projectb-rules` 스킬 | 코드를 쓰기/읽기/리뷰/위임하기 **전에** 읽는다 |
| 검증 규율 (헤드리스가 못 잡는 것·뮤테이션) | `projectb-verify` 스킬 | "테스트는 그린인데 게임이 안 된다"를 만나면 읽는다 |
| 아트 규격 (캔버스·시트·애니·팔레트·FX 기하) | `projectb-art` 에이전트 | 실측 기반. 도트 작업 착수 전 읽는다 |
| **왜 그렇게 판단했나** · 되풀이하면 안 되는 결정 | `docs/DECISIONS.md` | 큰 축을 건드리기 전에 읽는다 |
| **무엇을 바꿨나** (전문·날짜순) | `docs/CHANGELOG.md` | 새 변경은 여기 전문 + 아래 이력에 ≤200자 |
| 미정 수치 (증상 → 파일 → 현재값 → 선택지) | `docs/TUNING.md` | 사용자가 조인다 |
| **다음에 무엇을 할 것인가** | `docs/plans/NEXT.md` | 실기 확인은 `docs/TUNING.md` §13을 가리킨다(복제 안 함) |
| 착수한 작업 한 건 (왜·범위·설계·검증) | `docs/plans/active/*.md` | **한 작업 = 한 파일.** 끝나면 증류하고 `done/`으로 내린다 |

🔴 **문서는 시제로 갈린다 — 같은 사실을 두 시제에 쓰지 마라.** 과거(`CHANGELOG`) · 판단(`DECISIONS`) · 현재 값(`TUNING`) · 앞으로(`plans/`). 경계 한 줄: **다시 쓸 수 있는 판단이면 `DECISIONS`, 이번 한 번이면 `CHANGELOG`.**

⚠ **끝난 일의 체크박스를 남기지 마라.** 안 채운 `- [ ]`는 "아직 할 일"이라는 **거짓 신호**를 낸다 — `docs/archive/` 51건이 정확히 그 상태였고(일은 끝났는데 표시만 안 됨), 2026-07-29에 코드 대조 후 삭제했다.

⚠ **미러가 하나 남아 있다** — 플랫폼·엔진·렌더러·아트 스타일은 이 문서 최상단과 `docs/GDD.md` §3이 **미러**다. 한쪽을 고치면 다른 쪽도 같이 고친다.

---

## 🔴 네트워크 리뷰는 항상 필요하다

웹 게임이라 네트워크(WebRTC/WebSocket·동기화·권한·지연/재접속)가 코어다. **네트워크에 닿는 코드는 예외 없이 `projectb-netreview`를 거친다.** 브라우저는 ENet(UDP)을 못 쓰므로 전송 경로·신뢰 경계·권한 검증을 매번 본다.

이 관문에서 Critical이 반복적으로 잡혔다 — 호스트 자기 공속 누락 · 픽업 확정 로컬 반영 누락 · 직결 시 릴레이 유휴 절단 · 각 축 지연 보상 전무 · 속사수가 클릭 3타를 영영 못 냄.

---

## 하네스

**에이전트 5종** (`.claude/agents/`) · **스킬 32종** (`.claude/skills/`). 라우팅 정본 = `projectb-rules` §6.

### 트리거 규칙
- 코드를 **쓰거나·읽거나·리뷰하거나·서브에이전트에 위임하기 전에** `projectb-rules`를 읽어라.
- 헤드리스 테스트를 돌리거나 "테스트는 그린인데 게임이 안 된다"를 만나면 `projectb-verify`를 읽어라.
- `docs/GDD.md`를 쓰거나 고치기 전에 `projectb-gdd`를 읽어라.
- 큰 축(아트 규격·성장 축·신뢰 경계·공격 리듬)을 바꾸자는 제안이 오면 `docs/DECISIONS.md`를 먼저 읽어라.
- 단순 질문은 스킬 없이 직접 응답 가능.

### 에이전트
| 누가 | 무엇 |
|---|---|
| `projectb-dev` | 구현 — GDScript + **설계 스케치·Control UI·애니 배선·2D 셰이더·성능 진단까지 한 몸**. 큰 기능은 코드 전에 설계부터 낸다 |
| `projectb-reviewer` | 코드 리뷰 — 품질·하드 계약·이식성. 네트워크에 닿았으면 **netreview로 인계** |
| `projectb-netreview` | 🔴 네트워크 리뷰 — 전송 경계·호스트 권한·신뢰 경계·채널 분류·지연 보상 |
| `projectb-critic` | 기획 점검(레드팀) — 모순·공백·범위·안 닫히는 루프. **GDD는 수정하지 않는다** |
| `projectb-art` | 도트 에셋 — PNG(+ `_frames.tres` 제안)까지. `--import`·커밋은 리드 |

**리드가 직접 하는 것:** core 스키마 변경 · `mcp__godot` 에디터 제어 · `--import` · git 커밋 · 회귀 위험이 큰 tight 검증 루프.

✅ **`mcp__godot__*`(Godot MCP)는 쓴다** (2026-07-29 사용자 결정 — 그전까지는 "안 쓴다"였다). 에디터 인스턴스가 **하나뿐**이라 리드가 잡는다: 서브에이전트가 동시에 제어하면 열린 씬·선택 노드가 엇갈려 조용히 엉뚱한 노드를 고친다. 서브에이전트는 필요하면 **리드에게 요청**해라.

⚠ **삭제된 에이전트: planner · architect · ui · animator · shader · profiler · tools.** 없어진 이름을 부르지 마라 — 고유 지식은 `projectb-rules` §5 · `projectb-dev` · `projectb-gdd`로 이관했다.

⚠ **"AgentTool을 쓰지 마라"류의 지시를 에이전트 금지로 읽지 마라** — `projectb-*` 위임은 **기본값**이다.

### 기획은 에이전트가 아니라 파이프라인이다
| 단계 | 누가 | 무엇 |
|---|---|---|
| ① 브레인스토밍 | **메인 세션** (리드+사용자) | **위임하지 않는다** — 서브에이전트는 사용자와 직접 대화를 못 해 빈칸을 상상으로 채운다 |
| ② 점검 | `projectb-critic` | 모순·공백·범위·안 닫히는 루프를 적대적으로 뜯음 |
| ③ 문서화 | 리드 + `projectb-gdd` 스킬 | 🔒 사용자 승인 후 GDD에 기록 |
| ④ 구현 | `projectb-dev` | 코드 |

---

## 검증 명령 (정본)

상세·함정·해석은 `projectb-verify`. 여기는 **명령만** 둔다.

| 무엇 | 명령 |
|---|---|
| 🔴 스위트 전량 | `bash scripts/run_tests.sh` — **Bash 툴에서** (PowerShell은 자식 stdout을 안 보여준다). `--fast` · 이름 조각 필터(`run_tests.sh combat`) |
| 하네스 문서 규율 | `bash scripts/check_harness.sh` — 이력 길이·건수·소유권 표 링크 검사 |
| 🔴 같은 집 두 PC | `bash scripts/dev_local.sh` — 로컬 릴레이 :9080 + 웹 :8910 (RTT **13.8ms** vs 공용 릴레이 140~215ms). `--fast` · `--stop` |
| 웹 배포 | `bash scripts/deploy_web.sh` — 계정 보고 자동 분기 |
| 지연 진단(스위트 아님) | `tests/measure_latency.gd` — "렉이 있다" 신고 시 추측 전에 먼저 잰다 |
| 중계 서버 로컬 | `./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://server/relay/relay_server.gd -- --port=9080` |

🔴 **판정 3조건을 전부 본다:** `exit 0` + `TEST_OK` ≥ 1 + `SCRIPT ERROR` 0. `grep TEST_OK`만 하면 「침묵 통과」를 놓친다(실제로 `exit 0 / TEST_OK=1 / SCRIPT_ERROR=8`을 잡았다).

🔴 **씬 글루 파스 체크가 스위트보다 먼저 돈다** — `player.gd`·`combat_authority.gd`·`peer_sync.gd`·`boss.gd`는 `-s`가 preload를 안 해 **문법 오류·없는 함수 호출조차 스위트가 통과시킨다.** `run_tests.sh`가 자동으로 훑는다.

✅ **스위트 개수를 문서에 적지 마라** — `run_tests.sh`가 `tests/test_*_auto.gd`를 **자동 탐색**한다. 새 테스트는 파일을 놓는 것만으로 들어온다.

⚠ **헤드리스가 구조적으로 못 잡는 것이 있다** — 클릭 도달·렌더·시간 경과·GLSL 컴파일·지연(`dev_local.sh` 13.8ms로는 배포본 갈래가 안 돈다). 실기 목록 = `docs/TUNING.md`.

---

## 배포

- **주소:** `https://game.jachana.com` (`?host` / `?join=코드`) + `wss://relay.jachana.com`
- **양쪽 다 같은 명령: `bash scripts/deploy_web.sh`** — `wrangler whoami` 계정을 보고 자동 분기(jachana 계정 → 고정 주소 / 그 외 → 자기 `*.workers.dev`).
- **2인 체제:** djgnfj(이 머신) = 도메인·CF 계정 소유 · b-hy\*(친구) = workers.dev 임시배포. 릴레이는 친구가 배포하지 않는다(공용 `wss://relay.jachana.com`이 클라 기본값).
- ⚠ djgnfj 머신에서 game-worker를 **수동** deploy할 땐 반드시 `--config wrangler.jachana.jsonc` — 기본 jsonc엔 routes가 없다. 두 jsonc는 routes만 다르고 나머지는 미러다.
- 상세·함정(웹 익스포트 · `thread_support=false` · `exclude_filter` · 템플릿 경로) = `projectb-rules` §2.

---

## 커밋 규약

- **메시지는 한국어.** (트레일러 `Co-Authored-By`·`Claude-Session`은 형식이라 영어 그대로)
- **요약 줄은 `동사: 내용`.** 접두 동사는 이 목록에서만: `추가:` `변경:` `수정:` `삭제:` `문서:` `정리:`
- **한 커밋 = 한 논리 변경.** 성격이 다른 변경이 워킹 트리에 섞여 있으면 **한 번에 커밋하지 말고 먼저 물어라.** `git add -A` 전에 `git status`로 무엇이 섞였는지 본다.

---

## 현재 상태

**master 머지 + 배포 완료:** 전투 코어 · 챕터1(전투 칸→모닥불→보스) · 손맛/오디오 · **3직업 3무기**(전사 대검 · 궁수 활 · 법사 지팡이) · 드랍→제작/강화 루프(코드·**데이터 둘 다** 닫힘) · 성장 2축(직업 레벨 + 하위 직업 11장 조립) · 지연 보상 + **P2P 직결** · 근접 3모션(검·도끼·창) + 공격 리듬(선딜·후딜) + **칼끝 리본·칼 잔상 궤적**.

- **아트 규격 = 캐릭터 32px** (2026-07-27 복귀 · 카메라 ZOOM 1.0). 규격 정본 = `projectb-art` · 되돌린 근거 = `docs/DECISIONS.md`.
- **챕터1 보스 = "몰락한 망령"(64px) 하나.** `data/enemies/wraith_boss.tres` · `src/enemies/boss.gd`는 **어떤 `BossDef`든 돌리는 데이터 주도 공용 배우**(새 보스 = `.tres` + 시트 한 장). 예고는 **텍스처가 아니라 셰이더**(`assets/shaders/boss_telegraph.gdshader`). ⚠ 거대 악어는 2026-07-26에 완전 제거 — 옛 이력에 나오면 그 시점 기준이다.
- 🔴 **무엇을 할 차례인가 = `docs/plans/NEXT.md`.** 지금은 실기 확인(항목은 `docs/TUNING.md` §13) — A-1(**궤적 리본 + 칼 잔상**, 무기 4종)이 최우선이다. 에디터에서만 봤고 **웹은 처음**이다.
- **밀린 후보 = 아트 생산** — idle 2→4프레임 · 팔레트 2장 재작화 · 드랍 아이콘 재확인 · 죽은 세대 PNG 정리(`kobold_b`·`drake_c` 포함 — 두 적은 게임에 없다).

---

## 변경 이력

🔴 **세션당 ≤200자 · 최근 5건만.** 전문 = `docs/CHANGELOG.md` · 판단 근거 = `docs/DECISIONS.md`. 이 문서는 매 세션 로드되므로 **크기가 곧 비용**이다. 길이·건수는 `bash scripts/check_harness.sh`가 검사한다.

| 날짜 | 요약 (≤200자) |
|---|---|
| 2026-07-29 | **변경: 스윙 궤적을 칼끝 리본 + 칼 잔상으로.** 신고 3회의 원인은 값이 아니라 **형태**였다(셰이더는 「지나간 영역」, 칼은 「테두리 한 줄」 — 값으로 포갤 수 없다). 칼끝 좌표를 그대로 잇자 실측 1.03px. 셰이더·`swing_fx_*`·검기 파형 전량 삭제 |
| 2026-07-29 | **정리: 문서를 시제로 갈랐다 — 과거·판단·현재값·앞으로.** CLAUDE.md 43.7KB→9KB · `DECISIONS.md`·`plans/` 신설 · `archive`·`mockups`·`superpowers` 삭제(51건이 「끝난 일의 안 채운 체크박스」였다) · 18건 코드 대조 후 13건 회수 |
| 2026-07-28 | **추가: 근접 공격 리듬(선딜·후딜) + 궤적 진행 + `thrust`.** 판정을 클릭 프레임에서 「스윕 완료」로 — 그 순간 그려진 각이 판정을 항상 덮어 *"안 보이는데 맞는다"* 가 시간 축에서도 불가. 판정 기하 불변·DPS 중립. ⚠ 실기 미확인 |
| 2026-07-28 | **추가: 전사 근접 3모션(검·도끼·창).** 판정을 「원」에서 「무기별 부채꼴」로, 반각 = `swing_arc` 그 자체. netreview Critical 2(각 축 지연 보상 전무 · `or`로 대상 좌표 못 덮음). ⚠ 실기 미확인 |
| 2026-07-29 | **추가: 무기별 콤보 액션 — 연출에서 판정·화력으로.** 타수를 무기가 정함(도끼2·대검3·창4) + 창 마무리 = 돌진. critic이 전제를 뒤집음 — 활 콤보는 +21%가 아니라 **−3.6%(예산 중립)**였다. netreview Critical 1(각을 min된 타수로 세워 「로컬≤호스트」 파괴). ⚠ 실기 미확인 |
