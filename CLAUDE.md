# Project_B

> **정본은 두 곳이다 — 역할이 다르다.**
> - **기획 정본 = `docs/GDD.md`** — 게임이 "무엇인가"(비전·핵심 루프·메커니즘·콘텐츠). 🔒 **수정 시 사용자 승인 필수** (`.claude/settings.json` 권한 규칙으로 잠김). 쓰는 절차 = `projectb-gdd` 스킬, 점검 = `projectb-critic`.
> - **구현 정본 = 이 문서(CLAUDE.md)** — 하네스·아키텍처·검증 규율. 스킬·에이전트와 충돌하면 여기가 이긴다.
>
> **곁문서 (정본 아님 — 갱신 책임이 다르다):** `docs/CHANGELOG.md` 변경 이력 전문(새 변경은 **여기에** 전문으로 쓴다) · `docs/TUNING.md` 미정 수치(증상 → 파일 → 현재값 → 선택지, 사용자가 조인다) · `docs/archive/` 갱신이 멈춘 옛 작업 문서(참고용) · `docs/superpowers/specs/` 설계 초안(협동 보스 패턴 — ⚠ **초안은 남아 있지만 구현은 끝났다**: `src/stage/coop_authority.gd` + `stage_boss.tscn`의 `CoopAuthority` 노드 + `fx/coop_*` 9장 전부 배선됨).

- **정체:** 2D **픽셀아트 웹 게임** (코드네임 Project_B) — Godot 4.7.1, **Web(HTML5/WASM) 익스포트 타깃**, 렌더러 Compatibility(웹 필수). ⚠ 이 사실(플랫폼·엔진·렌더러·아트 스타일)은 `docs/GDD.md` §3과 **미러**다 — 한쪽을 고치면 다른 쪽도 같이 고친다.
- 🔴 **네트워크 리뷰는 항상 필요하다.** 웹 게임이라 네트워크(WebRTC/WebSocket·동기화·권한·지연/재접속 처리)가 코어다. 네트워크에 닿는 코드는 예외 없이 **`projectb-netreview`** 를 거친다(2026-07-26에 reviewer에서 전용 에이전트로 분리 — 점검 축이 7개로 깊어 일반 품질 리뷰에 끼우면 둘 다 얕아진다). 브라우저는 ENet(UDP)을 못 쓰므로 전송 경로가 맞는지, 신뢰 경계·권한 검증이 있는지 매번 본다.
- **엔진 실행 파일:** 루트의 `Godot_v4.7.1-stable_win64.exe`.
- **상태:** 기획 확정(GDD **v2.0**, 해커톤 마감 2026-08-10 — **남은 기간 약 2주**) · 아키텍처 규칙 확정(`projectb-rules` §1~§5). **아래는 전부 `master` 머지 + game.jachana.com 배포 완료** — 무엇을 왜 그렇게 했는지는 `docs/CHANGELOG.md`가 정본이다.
  - **전투 코어·챕터1(전투 칸→모닥불→보스)·손맛/오디오** · **3직업 3무기**(전사 대검 스윙 · 궁수 활 투사체 · 법사 지팡이 차지 폭발).
  - **드랍→마을 제작/강화→강해짐 루프가 코드상 닫힘** (드랍 등장·선착 픽업·제작대·강화·저장 롤백·I키 인벤·HUD 골드). ✅ **2026-07-27에 데이터상으로도 닫혔다** — 그전까지는 **도면 드랍원(브루트)이 어느 씬에도 배치돼 있지 않아** 3직업 전부 제작대에 도달할 수 없었다(보스도 `exp` 0·도면 0). 🔴 **"코드상 닫힘"을 루프가 돈다는 근거로 쓰지 마라** — 스위트는 계약을 지키지만 **데이터 도달성(그 적이 실제로 씬에 있는가)은 아무도 안 본다.** ✅ **2026-07-27에 성장 축도 3직업 전부 돈다** — 궁수·법사 하위 직업 6장 + `starting_sub_job_id`. 그전까지 `add_exp()`가 보유 하위 직업 0이면 즉시 반환해 **두 계열은 EXP를 한 점도 못 받았는데 에러가 없었다.** 이제 `test_game_state_auto`가 **계열 전수**로 지킨다(직업이 늘었는데 하위 직업을 안 만들면 빨개진다).
  - **성장 2축**: 직업 레벨(EXP → 캐릭터 스탯 5종 + 해금) · **하위 직업 조립**(장착 = 메인 1 + 서브 2 · 특성이 **자리별 두 얼굴** · 공유 하위 직업은 🔒서브 전용 · 특성 = **(효과 키, 값) 카탈로그** **8종** reach·roll_cd·roll_dist·campfire_heal·kill_move·drop_find·proj_range·**auto_fire**, 키마다 적용 지점 1곳 + `TRAIT_MAX` clamp). ⚠ **`auto_fire`만 on/off**다 — 나머지 7개는 비율이라 "합산 후 상한"이 성립하는데 스위치는 합산도 상한도 없다(`TRAIT_TOGGLE`·`is_trait_on`).
  - **네트워크**: 지연 보상(STRIKE 지연·위치 외삽·방어자 우대) + **P2P 직결**(게임 페이로드 WebRTC, 시그널링·폴백만 릴레이).
  - **손맛 개편 + 전 게임 16px 규격 전환**(4방향·대쉬 잔상·카메라 반동/ZOOM 2.0·평타 콤보·배경 드레싱).
  - 현 검증 = **헤드리스 스위트 8종 그린** (목록·실행법 = 아래 「검증 명령」 절 · `projectb-verify` §1). ⚠ 이 숫자는 아래 「검증 명령」의 "총 8종"과 **미러**다 — 새 `*_auto.gd`를 더하면 두 곳을 같이 고친다.
- 🔴 **아트 규격은 2026-07-27에 32px로 되돌렸다** (사용자 결정 — 배포본을 보고 "32가 더 좋았던 것 같다"). `bc2d178`(16px 전환) revert. **플레이어·잔몹 32×32**(시트 320×32 = 10프레임 · 잔몹 384×32 = 12프레임, **단방향** — 4방향 시트는 16px 세대에만 있었고 `player.gd:647`이 2방향으로 설계된 폴백을 한다) · 중형(브루트) **48** · **보스 64**(망령) · 아이템 16 · 바닥 타일 32 · 무기 형태별(대검 48×16 · 지팡이 40×14 · 활 18×34). 판정 기하도 함께 복귀(`attack_range` 42/48/56 등).
  - ✅ **카메라 ZOOM도 1.0으로 되돌렸다** (사용자 지적 "확대는 이전으로 안 돌아갔는데"). `ZOOM := 2.0`은 손맛 개편 자산이 아니라 **16px 보정으로 나중에 들어온 것**이었다(`a29991a`, 16px 전환 *이후*) — 32px 아트엔 줌이 아예 없었다. 함께 들어왔던 `SHAKE_MAX` 3.5→**7.0** · `KICK_MAX` 4.5→**9.0**도 되돌렸다(줌 좌표계 미러라 배율만큼 나눠 뒀던 값이다).
  - ⚠ **망령 보스(64px)는 16px 캐릭터 기준으로 만들어졌다** — 그때는 캐릭터의 4배였는데 지금은 **2배**라 "거대 보스" 인상이 약하다. 실기에서 판단하고, 키우려면 시트 재작화가 필요하다(현 2368×64 / 37프레임).
  - ⚠ 아래 「16px」 서술은 **2026-07-26 하루짜리 세대**의 기록이다 — 지금 기준이 아니다.
- 🗄️ **(옛 기록) 아트 규격 = `projectb-art` (2026-07-26 확정 — 캐릭터 16px).** 실측 기반으로 캔버스·시트 구성·애니 프레임/fps·팔레트 현황·FX 미러 상수를 그 파일에 박았다. 요약: 기준 해상도 **640×360**(`scale_mode=integer` → 1080p ×3·1440p ×4·4K ×6 전부 정수배라 **스팀 출시에도 재작화 불필요**) · 카메라 ZOOM 2.0(실효 월드 320×180) · **플레이어·잔몹 16×16**(플레이어 시트 160×48 = 10프레임×3방향, 잔몹 192×16 = 12프레임) · 중형 적 24 · **보스 64**(현행 망령) · 하위직업 아이콘 24 · 아이템 16 · 바닥 타일 32 · 디테일 8 · FX는 용도별(크기가 곧 판정 기하).
  - **16px를 고른 근거:** 핵심 재미가 협동 보스전(예고 보고 회피)이라 **가독성·시야**가 캐릭터 디테일보다 중요하고, GDD가 장비를 **수치만**으로 확정해 외형 차별이 없다. UI·로비는 이미 32px 아이콘을 따로 쓴다(인게임만 16px).
  - **팔레트 정본은 아직 없다** — 다만 실측상 **142개 에셋 중 126개가 20색 이하**로 규율은 이미 지켜지고 있다. 전체 색을 어지럽히는 것은 **`stage/boss_arena.png` 한 장(2206색 노이즈)** 과 `swamp_ground.png`(167색)뿐이라, 정본화 = 전 에셋 리맵이 아니라 **2장 재작화**다.
- **챕터1 보스 = "몰락한 망령"(64px) 하나다** — `data/enemies/wraith_boss.tres` · 씬 `src/enemies/boss.tscn`(노드 `Boss`) · 스크립트 `src/enemies/boss.gd`(**어떤 `BossDef`든 돌리는 데이터 주도 공용 배우** — 새 보스 = `.tres` + 시트 한 장). ⚠ **거대 악어는 2026-07-26에 완전 제거했다**(아트·데이터·`boss_croc` 이름 일체) — 미배선 악어와 배선된 망령이 공존해 문서·코드가 계속 혼선을 만들었다. 옛 이력(`docs/CHANGELOG.md`·메모리)에 "거대 악어"가 나오면 그 시점 기준이다. ✅ **알려진 결함 3건은 2026-07-26에 전부 해소됐다** (셋 다 "에러 없이 화면만 어긋나는" 부류였다 — 상세는 `projectb-art` 「보스」 절, 계약은 `projectb-rules` §3): ⑴ 콘 텔레그래프 68.6° 텍스처 vs 85.9° 판정(좌우 각 8.6° = range 130에서 **19px 무예고 피격**) → 재작화로 한 번 맞췄다가, **2026-07-27에 텍스처째 폐기하고 셰이더가 데이터에서 직접 그리게 바꿨다**(아래 별도 줄) ⑵ 망령 `walk` 없음(이동 시 idle 미끄러짐) → 4프레임 추가 ⑶ **공격 애니가 예고의 3분의 1에서 끝나** 보스가 휘두름을 마치고 idle 자세로 0.8~1초 서 있다가 때렸다 → `boss.gd`가 `speed_scale`로 애니 길이를 매 회차 예고 길이에 맞춘다(+ 아트가 공격 3종을 5→8프레임 보강). 🔴 **⑶은 데이터로 고칠 수 없었다** — 호스트의 예고 길이가 `telegraph_s + strike_delay_s(RTT)`로 **가변**이라 `.tres` speed로는 원리적으로 못 맞춘다. 그래서 §3의 "애니 길이 ≈ telegraph_s" 미러 계약은 **폐기하고 코드 유도로 대체**했다. 새 계약 = 공격 애니 `loop=false`(netreview가 미등록 계약으로 적발, `tests/test_boss_data_auto.gd`가 전수 단정).
- 🔴 **보스 예고(텔레그래프)는 이제 이미지가 아니라 셰이더다 (2026-07-27, 사용자 판단).** 형태·각·반지름을 `assets/shaders/boss_telegraph.gdshader`가 `.tres`의 `range`·`half_angle`에서 **직접 그린다** — `BossPatternDef.telegraph_tex` 필드와 `telegraph_cone.png`를 **삭제**했다. 계기는 사용자 질문 *"이런게 필요함? 이미지 안하지 않아 범위표시?"* 였고, 위 결함 ⑴이 정확히 "각이 픽셀에 박혀 데이터와 갈라진" 것이라 **재작화로는 `half_angle` 튜닝 때마다 재발**하는 구조였다. 이제 갈라짐이 **원리적으로 불가능**하고(리뷰 430만 표본 독립 대조: 무예고 0 · 오차는 과예고 방향으로만 최대 2.83px), 「두 번째 콘 패턴 게이트」도 함께 닫혔다(콘은 몇 개든 `.tres`만 늘리면 된다). 계약 = `projectb-rules` §3 「보스 텔레그래프 표시 기하」 · 튜닝 노브 = `docs/TUNING.md` §8. ⚠ **잔몹 예고는 여전히 텍스처**(`mob_melee.gd`의 `TELEGRAPH_TEX_SIZE` 미러) — 의도된 범위 제한이다. 🔴 **셰이더 실물은 아직 브라우저에서 안 봤다**(헤드리스는 GLSL을 컴파일하지 않는다) — 실기 확인이 남았다.
- ✅ **하위 직업 11장 완성 (2026-07-27) — 3계열 × 3 + 공유 2.** 궁수 사수·속사수·저격수 / 법사 술사·비전학자·마도사. 특성 키는 **7종**(`reach`·`roll_cd`·`roll_dist`·`campfire_heal`·`kill_move`·`drop_find` + **`proj_range`**). 🔴 **`proj_range` 상한 0.5의 근거는 `MAX_ARROW_RANGE`(480)이지 화면 해상도가 아니다** — 640×360이 GDD §9에서 아직 TBD라, 거기서 유도하면 미확정 값이 판정 기하의 근거로 승격된다. ⚠ **5스탯 값 집합은 세 계열이 완전히 동일하고 배치만 다르다** — `max_level_stats()`가 계열별 독립이라 예산 트립와이어가 **구조적으로** 안 터진다(실측 0.195/0.470/0.225/0.140/0.059). 새 하위 직업을 만들 때 이 규율을 깨면 예산을 다시 역산해야 한다.
- 🔴 **공격 리듬은 「무기」가 정한다 — 하위 직업이 아니다 (2026-07-27 사용자 확정).** 궁수 = **평·평·쭉 3타**(`EquipDef.combo_range_mult`/`combo_damage_mult`/`combo_delay`, 150→300px) · 법사 = **낡은 지팡이 마법볼(shoot) / 철 지팡이 차지(charge)** 로 갈라 **차지가 곧 제작 보상**이다. 원칙 = **직업은 성격(특성·5스탯) · 무기는 수치 + 공격 형태.** 🔴 **리듬을 특성 축에 두면 GDD §6 🔒("특성은 데미지에 곱해지지 않는 축만")을 어긴다** — 리듬 변경은 필연적으로 DPS를 건드린다. ⚠ **이 판단은 한 번 뒤집혔다가 되돌아왔다**: 리드가 "무기에 넣은 게 경계를 흐렸다"고 봤으나 `projectb-critic` 점검에서 **정반대**임이 드러났다(그 자리는 §6을 *지키려고* 고른 것이고 코드 주석에 이미 있었다). 축을 옮기려면 §6 🔒 개정 → 화력 예산 1.5배 유도 → 보스 HP 기준선까지 재산정이 딸린다. **다음에 같은 제안이 오면 이 줄을 먼저 읽어라.** 대안은 critic이 낸 **"무기가 허용 집합을 쥐고 하위 직업은 인덱스만 고른다"**(예산·신뢰 경계가 장비 축에 남는다).
  - 🔴 **콤보 타수는 사거리·데미지 배율을 고르므로 전사 근접 콤보(궤적만 정함)와 성격이 다르다 — 신뢰 경계가 새로 생겼다.** 호스트가 **자기 수신 간격으로 직접 세고** 클라 주장(G_SHOOT `cb`)은 **`min` 상한으로만** 쓴다 → **판정 ≤ 표시가 구조로 보장**돼 "화면에 없는데 맞는" 방향이 원천 차단된다(보스 예고의 "틀리면 과예고 방향으로"와 같은 규약). ⚠ 너무 빠른 발사는 **거부가 아니라 콤보 리셋**이다 — 거부하면 창 경계 지터로 **정당한 화살이 조용히 사라진다.**
  - ⚠ **`combo_range_mult`는 비감소여야 한다** — `min`이 지키는 것은 **타수** 부등호지 **거리** 부등호가 아니라서, `[2,1,1]` 같은 데이터 한 줄로 판정>표시가 열린다. `test_combat_math_auto`가 전수로 지킨다.
- ✅ **실기 확인 도구가 생겼다 (2026-07-28) — `?debug=1`에서 F1.** 직업·하위 직업 3칸·레벨·무기+강화를 즉시 바꾸고 「전부 열기」 한 번이면 만렙·전 장비 만강화·전 하위 직업이다. **「시험장으로」** = 허수아비 4마리(50·150·300·480px) + 바닥 거리 눈금, 클리어가 안 되는 씬이라 원하는 만큼 머문다. 🔴 **실기 절차를 지켜라 — ① 마을에서 F1로 세팅 ② 패널 닫기 ③ 시험장 이동 ④ 판 안에서 F1 열지 않기.** 판 도중 장비·레벨 변경은 편도 지연 동안 호스트가 옛 값으로 판정해 **데미지만 0**이 되는데, 그게 아래 홀드 연사 결함(I-1)과 **증상이 완전히 같아** 도구가 자기 측정을 오염시킨다. 상세 = `docs/TUNING.md` §11.
- 🔴 **다음 착수 = 실기 확인 (10항목, `docs/TUNING.md`).** 궁수 5 + 법사 5가 **"무엇이 보이면 실패인가"** 와 함께 정리돼 있다. ⚠ **지연 관련 항목은 `dev_local.sh`(13.8ms)로 영원히 재현되지 않는다 — 배포본(140~215ms)에서 봐야 한다.** ⚠ **배포 후 양쪽 다 하드 리로드(Ctrl+Shift+R)** — 구 캐시 클라가 섞이면 법사 공격이 **에러 없이 데미지만 0**이 된다(구 게스트 = 차지 샷 전량 거부 / 구 호스트 = 신 게스트 발사의 50% 거부). HUD·이펙트·소리가 정상이라 **적 HP가 안 깎이는 것**이 유일한 신호다.
- **다음 착수 후보(밀림) = 아트 생산.** 네 작업 모두 `assets/aseprite/`에 원본이 있어 **즉시 시작 가능**하다(2026-07-26 확인) — 남은 것은 아래 ①~④다. ⚠ **브루트가 `frames` 없이 `sprite` 한 장이라 걷기 애니가 없다**(정지 자세로 미끄러진다). 기존부터 그랬지만 07-27 배치로 **처음 화면에 드러났다** — 망령 `walk` 누락과 같은 부류이고, 여기서 아트 트랙과 만난다.
  - ① **idle 애니 2→4프레임**(3직업 6장) — 현재 2프레임 2.9fps라 두 장 깜빡임이다. 32px 재작화보다 싸고 체감이 크다. ⚠ 시트가 **160×48 → 192×48**로 늘어 `*_frames.tres` region 30개 재계산이 딸리고, 끝나면 `projectb-art` 애니 규격표(idle 2→4)도 갱신한다.
  - ② **팔레트 정본화 = 사실상 2장 재작화** — `boss_arena.png`(2206색 절차 노이즈)를 **32px 타일+디테일 구조**로 바꾸고 `swamp_ground.png`(167색) 정리. 나머지 126/142는 이미 20색 이하다.
  - ③ ⚠ **드랍 아이콘 "placeholder"는 재확인 대상** — 실측은 5~8색·채움 28~47%로 **정상 범위**다(옛 서술이 낡았을 가능성). `export_frame scale 8`로 눈으로 보고 판단한다. ⚠ **`ui/icon_weapon.png`를 "참조 0"이라 적었던 것은 오류다**(2026-07-26 정정) — `src/ui/inventory_panel.gd:14`가 **빈 무기 슬롯 픽토그램**으로 쓰고 있어 단순한 것이 정상이다.
  - ④ **죽은 세대 PNG 정리** — 규격 확정 전에는 16px/32px 실물 비교에 필요해서 남겼다. 이제 안전(목록·판별 명령은 `projectb-art`). 🔴 **참조 스캔에 `.gd`를 반드시 포함해라** — `.tres`/`.tscn`만 본 스캔이 살아 있는 에셋 3종(`fx/coop_*` 9장 · `items/blueprint.png` · `ui/icon_weapon.png`)을 "참조 0"으로 오분류했다(2026-07-26 정정).
  - ✅ **보스 결함 3건 = 해소 완료 (2026-07-26).** 망령 시트는 이제 **2368×64 / 37프레임 / 애니 6종**(idle 4 · walk 4 · swing·slam·spray 8 · death 5)이다. ⚠ **콘 텔레그래프 텍스처는 다음 날 폐기됐다**(2026-07-27, 아래). ⚠ **여기서 얻은 교훈 두 개가 다음 아트 작업에 그대로 적용된다:** ⑴ **콘 텍스처 계약은 조항이 넷**이다(정원 부채꼴·반지름=`tex_w`·전체각=2×`half_angle`·테두리 내측) — 문서가 "각도"만 적어 둬서 리드가 삼각형으로 오독했고, art가 실측으로 반박해 막았다 ⑵ **기하 검증은 "어느 알파 계층·어느 x 범위"를 밝혀라** — apex 근처 테두리 픽셀을 잡으면 정확한 텍스처가 2° 위반으로 보인다(리드가 그래서 불필요한 재작화를 지시했다가 철회).
- **그 외 남은 것:** 손맛 수치 실기 튜닝(`docs/TUNING.md`) · **실기 확인 몫**(웹 2클라: 드랍/픽업·제작 루프·모닥불·전멸→마을 롤백·IndexedDB 저장·SFX·I키 인벤 클릭 도달 · **P2P "직결" 표시 + 배포본에서 4분 이상 유지** · 4방향 전환·무기 그립·확대 배율·이동 체감).
  - 🔴 **보스 애니 타이밍(2026-07-26 수정분)은 헤드리스가 구조적으로 못 잡는다 — 실기 4항목**(netreview 지목): ⑴ **때리면서** 예고를 봐라 — 히트스톱 누적(약 0.26s)은 안 때리면 재현되지 않는다 ⑵ **같은 패턴 연속 2회**(swing 쿨 2.0s)에서 마지막 프레임 얼음이 없는지 — 보스가 이동 중(대상 거리가 `keep_distance` 150의 데드밴드 138~190px **밖**)이어야 겨눌 수 있다 ⑶ 🔴 **`dev_local.sh`(13.8ms)로는 핵심 갈래가 안 돈다** — `strike_delay_s`가 거의 0이라 호스트 배율이 1.0에 붙는다. 호스트/게스트 배율 차이는 **배포본(140~215ms)에서만** 보인다 ⑷ 페이즈2 + spray(burst 6)에서 N개 원 타이머와 애니가 같이 끝나는지.

## 하네스: Project_B (2D Godot)

**목표:** Project_B의 Godot 개발을 전문 에이전트 팀으로 나눠, 아키텍처 규칙·검증 규율을 일관되게 지키며 구현한다.

**트리거 규칙:**
- **코드를 쓰거나·읽거나·리뷰하거나·서브에이전트에 위임하기 전에 반드시 `projectb-rules` 스킬을 읽어라.** 아키텍처 규칙·모듈 지도·하드 계약·"조용히 깨지는 함정"이 여기 있다.
- **헤드리스 테스트를 돌리거나·"테스트는 그린인데 게임이 안 된다"를 만나면 `projectb-verify` 스킬을 읽어라.**
- **`docs/GDD.md`를 쓰거나 고치기 전에 `projectb-gdd` 스킬을 읽어라** (🔒 승인 절차·미러 규약·변경 이력 규율).
- 단순 질문은 스킬 없이 직접 응답 가능.

**하네스 = 에이전트 5종 + 스킬 32종 (2026-07-26 재편 — 쓰는 것만 남겼다):**
> ⚠ **"AgentTool을 쓰지 마라"류의 지시를 에이전트 금지로 읽지 마라 (2026-07-26 사용자 정정).** 이 프로젝트에서 쓰지 않는 것은 **Godot MCP(`mcp__godot__*`)** 이고, `projectb-*` 서브에이전트 위임은 **기본값**이다. 특히 **네트워크 리뷰는 상시 필수**(최상단 🔴). 실제로 v1.9 특성 축이 이 오독으로 리뷰 없이 머지될 뻔했다.

**기획은 에이전트가 아니라 파이프라인이다:**

| 단계 | 누가 | 무엇 |
|---|---|---|
| ① 브레인스토밍 | **메인 세션** (리드+사용자 대화) | 무엇을 만들지 정한다. **위임하지 않는다** — 서브에이전트는 사용자와 직접 대화를 못 해 대화가 한 겹 끊기고, 빈칸을 상상으로 채우게 된다 |
| ② 점검 | `projectb-critic` | 정한 것의 모순·공백·범위·안 닫히는 루프를 적대적으로 뜯음 (방법론 = `projectb-plancheck`) · GDD 수정 안 함 |
| ③ 문서화 | 리드 + **`projectb-gdd` 스킬** | 점검을 통과한 것을 GDD에 기록 (🔒 사용자 승인 후) |
| ④ 구현 | `projectb-dev` | 코드 |

**나머지 에이전트:**
- **구현 = `projectb-dev`** — GDScript + **설계 스케치·Control UI·애니 배선·2D 셰이더·성능 진단까지 한 몸**(구 architect·ui·animator·shader·profiler 흡수). 큰 기능은 코드 전에 설계부터 낸다.
- **코드 리뷰 = `projectb-reviewer`** — 품질·하드 계약·이식성 게이트. 네트워크에 닿았으면 **netreview로 인계**한다.
- 🔴 **네트워크 리뷰 = `projectb-netreview`** — 전송 경계·호스트 권한·신뢰 경계·채널 분류·지연 보상. **네트워크에 닿는 코드는 예외 없이 이 리뷰를 거친다.**
- **에셋(도트) = `projectb-art`** — ✅ 규격 정본이 그 파일에 있다(2026-07-26 확정, 캐릭터 16px). 착수 전 거기를 읽고 따른다.
- **리드가 직접 하는 것:** core 스키마 변경, `mcp__godot` 필요 작업, `--import`, git 커밋, 회귀 위험이 큰 tight 검증 루프.
- ⚠ **삭제된 에이전트: planner · architect · ui · animator · shader · profiler · tools.** 없어진 이름을 부르지 마라 — 고유 지식은 `projectb-rules` §5(UI 모달·4방향 애니·셰이더·웹 프로파일)·`projectb-dev`(설계)·`projectb-gdd`(GDD 작성)로 이관했다. 라우팅 정본 = `projectb-rules` §6.

**검증 명령 (정본):**
- 테스트는 **Bash 툴에서** 돌린다(PowerShell은 자식 stdout을 안 보여준다). 스위트 전체 명령은 `projectb-verify` §1이 정본 — 새 `tests/*_auto.gd`를 추가하면 여기와 `projectb-verify` §1을 **동시에** 갱신한다.
- 현재 스위트: `tests/test_net_transport_auto.gd`(**전송 계층 계약 — fast 채널 분류·사건 kind 제외·G_* 이름 유일성 전수·keepalive 부등식**) · `tests/test_net_room_auto.gd`(멀티 방 왕복 — 릴레이+호스트+게스트 3프로세스) · `tests/test_combat_math_auto.gd`(전투 신뢰 경계 단위 — 사거리·쿨다운·구르기 i-frame·잔몹 타격·화살/차지 발사·**메인 특성 사거리(항등·상한·기하 정합)**·**특성 카탈로그 clamp·구르기 파생(쿨/거리)·외삽 상한 불변식**) · `tests/test_game_state_auto.gd`(직업·챕터 리졸버 — data 스캔 allowlist + 조작 id/인덱스 거부 + 진행 좌표·HP 이월 + **자리별 특성 리졸브(메인/서브 두 얼굴·공유는 서브 전용·슬롯 초과·중복 폐기)·장착 슬롯·슬롯 기준 예산 트립와이어**) · `tests/test_health_component_auto.gd`(HP·부활 권한/표시 경로 격리) · `tests/test_save_manager_auto.gd`(커밋/전멸 롤백 — 클리어분 생존·전멸분 소실·무파일 첫 판 전멸, save_path 격리·GameState 주입 + **EXP/레벨 롤백·SAVE_VERSION 트립와이어**) · `tests/test_stage_dressing_auto.gd`(배경 드레싱 — 폴리지 회전 피벗=밑동·배치 결정론(호스트/게스트 동일 지면)·제외 영역·`collision_layer=0`) · `tests/test_boss_data_auto.gd`(**보스 데이터 계약 — `data/enemies/*.tres`의 `BossDef` 전수**: 패턴 id ↔ 동명 애니 존재 · 공격 애니·`death` `loop=false` · `idle`/`walk` 존재 · 애니 기본 길이 ≤ `telegraph_s`×1.5 · `telegraph_s > 0`. 전부 **에러 없이 화면만 어긋나는** 부류라 트립와이어가 유일한 방어다). **총 8종.** 실행법은 `projectb-verify` §1. 판정 = `TEST_OK` + exit 0 + `SCRIPT ERROR` 없음.
- 🔴 **같은 집 두 PC 테스트 = `bash scripts/dev_local.sh`** (로컬 릴레이 :9080 + 웹 :8910, LAN IP 자동 감지·안내 출력). 공용 릴레이는 한국→홍콩→한국을 왕복해 **RTT 140~215ms**인데 로컬은 **13.8ms**다(실측 2026-07-24) — 개발 중 반복 테스트는 반드시 이걸로. 클라가 페이지 호스트를 보고 릴레이를 자동 판별하므로 `?relay=` 부착 불필요(`Net.default_relay_url`). `--fast`(익스포트 생략) · `--stop`(Ctrl+C가 안 먹었을 때 정리). ⚠ 호스트도 **LAN 주소**로 열어야 초대 링크가 공유 가능하다(localhost로 열면 링크에 localhost가 박힌다). 배포본(`game.jachana.com`)은 심사위원·원격 친구용으로 그대로.
- **지연 진단(스위트 아님):** `tests/measure_latency.gd` — 릴레이 왕복 RTT + 회피 창 예산 환산. "렉이 있다·게스트만 맞는다" 신고 시 추측 전에 먼저 잰다. 실행법·해석은 `projectb-verify` §1.
- 중계 서버 로컬 실행: `./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://server/relay/relay_server.gd -- --port=9080`
- **실배포(탈PC, 로컬 프로세스 0개):** 릴레이 = Cloudflare Workers(`server/relay-worker`, `npx wrangler deploy`), 웹 = 정적 서빙 Worker(`server/game-worker`) — **웹 배포 정본 = `bash scripts/deploy_web.sh`**(익스포트→wasm gzip 스테이징→deploy). 주소 = `https://game.jachana.com`(?host / ?join=코드) + `wss://relay.jachana.com`. 상세·함정은 `projectb-rules` §2 server. (구 `scripts/start_multi.bat` 터널 방식은 로컬 개발 폴백)
- **웹 익스포트:** `./Godot_v4.7.1-stable_win64.exe --headless --path . --export-release "Web" build/web/index.html` → `cd build/web && python -m http.server 8910` 후 브라우저에서 `http://localhost:8910` 확인.
  - 웹 템플릿 필요: `%APPDATA%/Godot/export_templates/4.7.1.stable/` (web_*.zip — 설치돼 있음).
  - `export_presets.cfg`는 **커밋돼 있다**(웹 프리셋뿐, 비밀값 없음 — 클론만으로 익스포트 가능). `thread_support=false` 필수, `exclude_filter`에 `.mcp.json, .claude/*, docs/*, memory/*`. 모바일 프리셋(서명 비밀번호)을 추가하게 되면 그때 다시 gitignore로 분리.

**협업 배포 (2인 체제 — "배포해줘" 한마디로 끝나야 한다):**
- **djgnfj** (이 머신) = jachana.com 도메인·Cloudflare 계정 소유자. **b-hy\*** (친구) = 도메인 없음 → workers.dev 임시배포.
- **양쪽 다 같은 명령이다: `bash scripts/deploy_web.sh`.** 스크립트가 `wrangler whoami` 계정을 보고 자동 분기 — jachana 계정이면 `wrangler.jachana.jsonc`(고정 주소 `game.jachana.com`), 그 외 계정이면 기본 `wrangler.jsonc`(routes 없음 → 자기 `*.workers.dev` 임시 주소).
- ⚠ djgnfj 머신에서 game-worker를 **수동으로** deploy할 땐 반드시 `--config wrangler.jachana.jsonc` — 기본 jsonc엔 routes가 없다. 두 jsonc는 routes만 다르고 나머지는 미러 — 설정을 바꾸면 둘 다 고친다.
- 친구 사전 준비(1회): Godot 4.7.1을 레포 루트에 다운로드 + 웹 익스포트 템플릿 설치 + `npx wrangler login`(무료 계정). `export_presets.cfg`는 커밋돼 있어 클론만으로 익스포트 가능.
- 릴레이는 친구가 배포하지 않는다 — 공용 `wss://relay.jachana.com`이 클라이언트 기본값(`Net.DEFAULT_RELAY_URL`)이라 workers.dev 임시배포·네이티브 클론 실행 모두 `?relay=` 없이 바로 붙는다. (릴레이 배포 = djgnfj 전용, `server/relay-worker`.)
- djgnfj는 친구가 보낸 workers.dev 링크(`…?host`)를 브라우저로 열기만 하면 됨 — 로컬 설치·실행 불필요. 심사위원은 README.md의 "바로 플레이/클론해서 실행" 참조.
- ✅ workers.dev 분기 실기 검증 완료 (2026-07-23, b-hy 첫 배포 성공 → `https://projectb-game.youqlrqod.workers.dev`). 이때 발견한 함정 2건을 해소함: 웹 익스포트 템플릿 미설치(→ 설치 완료), `build/web` 폴더 부재로 익스포트 실패(→ deploy_web.sh에 `mkdir -p` 추가).

**커밋 규약:**
- **메시지는 한국어로 쓴다.** (요약 줄 + 필요 시 본문. 트레일러 `Co-Authored-By`·`Claude-Session`은 형식이라 그대로 영어로 붙인다.)
- **요약 줄은 `동사: 내용` 형식.** 접두 동사는 아래 목록에서만 고른다:

  | 접두 | 쓰임 |
  |---|---|
  | `추가:` | 새 기능·파일·에이전트·스킬 |
  | `변경:` | 기존 동작·기획을 바꿈 (예: `변경: 성장 방식을 레벨업 → 장비 강화로`) |
  | `수정:` | 버그 고침 |
  | `삭제:` | 제거 |
  | `문서:` | 문서·주석만 손댐 |
  | `정리:` | 리팩터·포맷 (동작 변화 없음) |

- **한 커밋 = 한 논리 변경.** 성격이 다른 변경(예: 에이전트 추가 + 버그 수정 + 문서 정리)이 워킹 트리에 섞여 있으면 **한 번에 커밋하지 말고 먼저 물어라** — 어떻게 쪼갤지 사용자에게 확인받은 뒤 나눠 커밋한다. `git add -A`로 전부 쓸어담기 전에 `git status`로 무엇이 섞였는지 본다.

**변경 이력:** 🔴 **전문(날짜순 전체) = `docs/CHANGELOG.md`.** 새 변경은 **거기에 전문으로** 쓰고 아래 목록 맨 위만 갱신한다 — 이 문서는 매 세션 로드되므로 전문을 여기 쓰면 다시 비대해진다.

| 날짜 | 최근 5건 (전문 = `docs/CHANGELOG.md`) |
|------|------|
| 2026-07-28 | **추가: 속사수 홀드 연사(`auto_fire`) + F1 디버그 패널·시험장** — 홀드 = 균일 연사 / 클릭 = 평·평·쭉이 **플레이어의 선택**으로 남는다(홀드 DPS는 클릭의 92% = 편의의 대가). 🔴 **첫 on/off 특성**이라 카탈로그가 비율 일색이던 전제가 깨졌다. 신뢰 경계는 `authoritative_combo`의 `min`이 자동으로 닫는다(새 메시지 0). 도구 = `?debug=1` + F1(전부 열기·시험장 이동) · 시험장(허수아비 4 + 거리 눈금) · **rules §2 허수아비 게이트 해소**. 🔴 **netreview가 Critical 2건을 잡았다** — C1: `_stop_auto_fire`가 콤보를 지워 **속사수가 클릭으로도 3타를 영영 못 냈다**(cb가 항상 0이라 desync·에러 없이 스위트 초록) · C2: 홀드가 호스트 발사율 게이트 경계(여유 15ms)에 못 박혀 **게스트만** 화살이 산발 거부(화살은 그려지고 데미지만 0). 처방 = **클라가 물러선다**(게이트를 넓히면 스팸 상한이 같이 오른다). ⚠ **I-1 잔여**: 여유는 프레임 양자화만 막고 수신 버스트는 못 막는다 — 진짜 처방은 leaky bucket(TUNING §9). ⚠ **실기 전량 미확인** |
| 2026-07-27 | **추가: 원거리 사거리 특성(`proj_range`)과 궁수·법사 공격 리듬 분화 — 3직업 전부 성장 축이 돈다** — 하위 직업 6장 + 아이콘 6장 + `starting_sub_job_id`. 그전까지 두 계열은 **EXP를 한 점도 못 받았는데 에러가 없었다.** 궁수 = **평·평·쭉 3타**(150→300px) · 법사 = **마법볼 / 차지는 제작 보상.** 🔴 **리듬은 무기가 정한다** — 특성 축에 두면 §6 🔒을 어긴다(리드가 반대로 판단했다가 critic 점검에서 뒤집혔다). 콤보 타수가 사거리·데미지를 고르므로 **신뢰 경계 신설** — 호스트가 직접 세고 주장은 `min` 상한만 → **판정 ≤ 표시 구조 보장.** **트립와이어 5개**(`.tres` 33개 전수 로드·계열 전수·콤보 배율 비감소·씬 배선 전수·발사형 사거리 명시) — 전부 "에러 없이 조용히 깨지는" 부류다. 검증 = 스위트 7종 · **뮤테이션 10건** · netreview 3회(Critical 0) · 기획 점검 1회 · 익스포트 ERROR 0. ⚠ **실기 10항목 미확인** |
| 2026-07-27 | **추가: 챕터1 루프를 데이터상 도달 가능하게** — 🔴 **유일한 도면 드랍원인 브루트가 어느 씬에도 배치돼 있지 않았다**(`src/` 참조 0건). 보스도 `exp` 필드가 없어 EXP 0·도면 0이었다. 즉 "루프가 **코드상** 닫힘"은 참인데 **데이터상 3직업 전부 제작대에 도달 불가**였다. 브루트 배치 + 보스 `exp 30`·도면 3장 + `sharp_fang` 드랍 + 원거리 무기·레시피 4장 + 궁수·법사 `exp_curve`(폴백이라 요구치가 **2.4배**였다) = **9파일, GDScript 0줄**. 1회 완주 120 EXP → Lv4로 GDD §6 페이싱 일치. 🔴 **교훈: "코드상 닫힘"을 루프가 돈다는 근거로 쓰지 마라** — 스위트 8종은 내내 그린이었다. 테스트는 계약을 지키지만 **데이터 도달성은 아무도 안 본다.** ⚠ 궁수·법사 성장 축은 하위 직업 6장(다음 착수)까지 안 돈다 |
| 2026-07-27 | **변경: 보스 예고를 이미지에서 셰이더로 — 기하가 데이터에서만 온다** (사용자 질문 "이런게 필요함? 이미지 안하지 않아 범위표시?"에서 출발). 전날 재작화로 고친 콘 각 결함이 **`half_angle`을 튜닝하면 재발하는 구조**임이 드러나, 텍스처를 폐기하고 `boss_telegraph.gdshader`가 `range`·`half_angle`에서 직접 그리게 했다. `BossPatternDef.telegraph_tex` 필드 + `telegraph_cone.png` **삭제**. 🔴 **부수 발견: 그 필드가 남긴 게이트("null이면 예고 생략, 판정은 진행")는 데이터 한 칸을 비우면 무예고 피격 100%가 되는 경로**였다 — 없애려던 결함 클래스 그 자체라 필드째 제거. 리뷰 430만 표본 독립 대조로 무예고 0·오차는 과예고 방향으로만 최대 2.83px 실증 · 「두 번째 콘 패턴 게이트」 해소 · `modulate` 무시 함정(I1) 적발·수정 |
| 2026-07-26 | **수정: 보스 결함 3건 일괄 해소 (콘 각·`walk`·공격 애니 타이밍)** — 셋 다 "에러 없이 화면만 어긋나는" 부류. 🔴 **⑶은 데이터로 고칠 수 없었다** — 예고 길이가 `telegraph_s + strike_delay_s(RTT)`로 가변이라 `.tres` speed로는 원리적으로 불가 → `_telegraph_duration()` 단일 소스 + `speed_scale` 유도로 **미러 계약을 코드 강제로 전환**. 부수 함정 2건(`hit_stop`의 `speed_scale` 리셋 · `_play`가 같은 애니를 리스타트 안 함)도 막고 **`tests/test_boss_data_auto.gd` 신설(스위트 7→8종)** — `tests/`에 `boss`가 0개여서 기존 스위트는 회귀 없음만 증명했다. 🔴 **진짜 발견은 문서 결함**: §3 콘 계약이 각도만 적어 형태·반지름 조항을 빠뜨려 리드가 삼각형으로 오독(art가 실측 반박으로 막음) → **4조항**으로 재작성 |
| 2026-07-26 | **하네스 전수 점검 — 문서가 코드보다 뒤처진 곳 16건 동기화** — 3축(에이전트 5·스킬 4·CLAUDE.md+스킬 32)을 **코드 실측**으로 검증. 고장 패턴은 하나였다: *한 사실이 여러 곳에 복제돼 한쪽만 갱신된다*(아트 규격·외삽 상한 56→115·특성 v1.9 함수명·planner 승인자·스위트 개수). `CoopAuthority` 등 권한 컴포넌트 5종이 rules §2에 통째로 누락된 것도 적발. 🔴 **보스 공격 애니 ↔ `telegraph_s` 미러 깨짐(새 결함, 미해소)** 발견. 처방 = 수치를 문서에 복제하지 말고 코드를 가리키게 |
| 2026-07-26 | **삭제: 거대 악어 보스 일체 + `boss_croc` → `boss` 이름 일반화** — 미배선 악어와 배선된 망령이 공존해 문서·코드가 계속 혼선을 만들었다(파일 32개 삭제 · 씬/스크립트/노드명 일반화 · 문서 5곳 정정). 검증 = 스위트 7종 그린 + 웹 익스포트 ERROR 0 + `.import` 오염 0 |
| 2026-07-26 | **아트 규격 정본 확정 (캐릭터 16px)** — `projectb-art`에 캔버스·시트·애니 프레임/fps·팔레트 현황·FX 미러를 실측으로 박음 · 팔레트 실측(126/142가 20색 이하, 어지럽히는 건 `boss_arena.png` 2206색 한 장) · **보스 현행 = 망령**임을 확인하고 문서 정정 · 🔴 콘 텔레그래프 각 불일치(68.6° 텍스처 vs 85.9° 판정) 발견·등록 |
| 2026-07-26 | **하네스 재편 — 에이전트 11→5, 스킬 46→32** · 기획을 에이전트에서 **파이프라인**으로(브레인스토밍=메인세션 → 점검=critic → 문서화=`projectb-gdd` 스킬 신설 → 구현=dev) · **`projectb-netreview` 신설**(네트워크 리뷰 분리) · dev가 설계·UI·애니·셰이더·성능 흡수 · 삭제 전 고유 지식 6건을 rules §5로 이관 |
| 2026-07-26 | **하네스 정돈** — 변경 이력 43건을 `docs/CHANGELOG.md`로 이관(CLAUDE.md 71KB→14.8KB) · 스위트 목록 갈라짐 수정(6→7종) · **아트 규격 서술 제거**(합의 대기) · 에이전트 11종에 누락 규율 반영 · 고아 문서 `docs/archive/`로 |
| 2026-07-26 | **손맛 개편 + 전 게임 16px 규격 전환** — 4방향 표시·대쉬 잔상·카메라 반동/ZOOM 2.0·평타 콤보·배경 드레싱 3종·월드 Label 클릭 먹음 수정 + 아트 전면 16px 재작화·판정 기하 재조정. `tests/test_stage_dressing_auto` 신설, `docs/TUNING.md` 신설 |
| 2026-07-26 | **수정: 직업을 바꿔도 남의 직업 무기가 착용된 채 남던 것** — `apply_job_loadout()` 단일 소스 + 호스트가 공지 무기를 공지 직업과 대조(`can_job_equip`). 겉모습이 아니라 화력 예산 이탈이었다 |
| 2026-07-26 | **기획 v2.0 — 하위 직업 조립 축** — 장착 슬롯 메인 1 + 서브 2 · 특성이 자리별 두 얼굴 · 공유 하위 직업(서브 전용) · 특성 = (효과 키, 값) 카탈로그. 구현은 `feat/subjob-trait`에서 완료·master 머지 |
| 2026-07-26 | **P2P 직결 전환** — 게임 페이로드는 WebRTC DataChannel, 시그널링·폴백만 릴레이. 게임 코드 무변경(전송 경계가 `send_game`/`net_msg` 한 쌍). `tests/test_net_transport_auto` 신설 |
