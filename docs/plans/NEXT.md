# 다음에 무엇을 할 것인가

> **이 문서는 "앞으로"의 정본이다.** 지금 무엇을 하는 중이고, 다음에 무엇을 집을지가 여기 있다.
>
> 다른 시제는 다른 문서가 가진다 — **과거** = `docs/CHANGELOG.md`(무엇을 바꿨나) · **판단** = `docs/DECISIONS.md`(왜 그렇게 정했나) · **현재 값** = `docs/TUNING.md`(미정 수치) · **무엇을 만드는가** = `docs/GDD.md` 🔒.

## 쓰는 법

- 항목이 **끝나면 지운다.** 체크박스를 남기지 마라 — 안 채운 체크박스는 "아직 할 일"이라는 **거짓 신호**를 낸다(2026-07-29에 `docs/archive/` 51건이 정확히 그 상태였다: 일은 끝났는데 표시만 안 됐다).
- 착수한 것은 `docs/plans/active/YYYY-MM-DD-<slug>.md` 한 장으로 뗀다 — **왜 / 무엇(범위·비범위) / 어떻게(설계) / 검증 / 완료 조건**을 한 파일에 둔다. 설계와 계획을 두 장으로 쪼개면 갈라진다.
- 끝나면 **증류**한다: 무엇을 바꿨나 → `CHANGELOG` · 되풀이 금지 판단 → `DECISIONS` · 못 정한 수치 → `TUNING` · 기획 변경 → `GDD` 🔒. 그리고 파일은 `docs/plans/done/`으로 내린다. **plans는 참조 문서가 되지 않는다.**

---

## 1. 지금 착수 — 🔴 "멈춘다" 수정본 실기 확인

**원인은 확정·수정·배포됐다**(2026-08-01) — 씬 전환 프레임의 유령 노드가 `_ready` 일회 스캔에
박혀 `as` 캐스트에서 터졌고, 그래서 **스테이지2가 영영 클리어되지 않았다.** 전문 =
`docs/CHANGELOG.md` · 판단 = `docs/DECISIONS.md` · 재발 시 진단 = `docs/TUNING.md` §19.

🔴 **밟아야 할 것은 딱 하나 — `stage_1 → stage_2` 전환이다.**

| # | 무엇을 | 어긋나면 |
|---|---|---|
| **M-1** 🔴 | **stage_1을 클리어하면 stage_2로 넘어가고, stage_2도 클리어된다** | 3초 배너 뒤에 안 넘어간다 / 넘어갔는데 잡몹을 다 죽여도 그대로다(= 수정이 안 먹은 것 — 웹 콘솔 F12에 `Trying to cast a freed object`가 있는지 본다) |
| **M-2** 🔴 | **2인: 게스트 화면에서 stage_2 잡몹이 움직인다** | 얼어붙어 제자리에 있다(`G_MOB_POS` 미송신 — 같은 결함의 게스트 쪽 얼굴). ⚠ 이 경로는 stage_2에서 **한 번도 안 돌아 본 코드**다 |
| **M-3** | **stage_2 → 모닥불 → 보스까지 완주** | 모닥불에도 CombatAuthority가 있어 같은 오염 경로를 지난다(그쪽은 `_check_clear`가 안 불려 조용했다) |

⚠ **§18 A절(물·낭떠러지 실기)이 이 결함에 가려 있었을 수 있다** — stage_2가 클리어 안 되는 상태로
테스트했다면 그 절의 관찰은 못 믿는다. 같이 다시 밟는다.

## 2. 그다음 — 실기 확인 (배포는 이미 나갔다)

목록 정본 = `docs/TUNING.md`. **웹 빌드는 이미 여러 번 돌렸으니** 이제 브라우저에서 항목을 짚기만 하면 된다.

| 절 | 무엇 | 최우선 |
|---|---|---|
| **§18** | 전투 스테이지 개편 — 잡몹 3종·못 가는 지형·원거리 궁수 | **A-1 물·낭떠러지에 실제로 막히는가** — 게임 코드 변경 0으로 붙은 것이라 사람이 한 번도 안 밟아 봤다 |
| §17 | 공격 리듬 — 선입력 버퍼 + 칼날 폭 리본 | 전량 미확인 |
| §16 | 마을 숲 아트 | F-8 웹 빌드 |
| §13 | 근접 모션 개편 | A-1 궤적 리본·칼 잔상 |

🔴 **B절(2인)은 전부 배포 릴레이에서만 드러난다** — `dev_local.sh`는 편도 6.9ms라 지연 갈래가
한 줄도 안 돈다. 착탄 vs HP 감소 순서 · 게스트 화살 위치 · 물 건너 사격 차단이 거기 걸려 있다.

## 3. 밀린 것

- **궁수·법사 코드 경로 정리** — 데이터·에셋·UI는 2026-08-01에 지웠고 `motion_type` shoot/charge
  분기와 차지 로직이 남아 있다. 🔴 **몹 화살이 `ArrowField._instantiate`를 공유**하므로 그냥
  들어내면 안 된다 — 공용부를 먼저 가르는 작업이다.
- **테스트 공백 하나** — 「세이브 로드 비파괴」가 직업이 하나뿐이라 재현 불가다(`test_game_state_auto`에
  주석으로 표시). 두 번째 직업이 생기면 되살린다.
- **아트 생산** — idle 2→4프레임 · 팔레트 2장 재작화 · 드랍 아이콘 재확인.
- **`docs/plans/active/2026-07-31-minotaur-boss-patterns.md`** — 내용은 전부 완료인데 `active/`에
  남아 있고 "미커밋" 절이 낡았다. `done/`으로 내릴지 확인 필요.

## 1.5. 마을 숲 아트 — 끝났다 (웹 확인만 남음)

생산·배선·팔레트 통일·트립와이어 + **에디터 실기 1차**까지 완료(2026-08-01, 전문 = `docs/CHANGELOG.md`). 1차에서 길 폭·경계 물결·장식 그림자 3건을 고쳤고 **카메라 줌은 1.0 유지**로 확정했다(실물 비교 후 사용자 결정).

🔴 **남은 것 = 웹 빌드 확인(F-8)과 F 상호작용(F-4)** — 목록 정본 = `docs/TUNING.md` §16. 조일 노브도 거기 있다.

## 2. 밀린 후보 — 아트 생산

정본 = `CLAUDE.md` 「현재 상태」. idle 2→4프레임 · 팔레트 2장 재작화 · 드랍 아이콘 재확인 · 죽은 세대 PNG 정리.

---

## 3. 비주얼 폴리시 백로그

> 출처 = `docs/archive/QUALITY_BACKLOG.md`(2026-07-23). 2026-07-29에 18건을 **코드로 전수 대조**해 살아있는 것만 회수했다 — 그 문서는 삭제됐고 여기가 정본이다.
>
> **해결 확인 5건:** A1 Y-소트(`village.tscn` 5곳) · A2 마을 바닥 1.5× 비정수 스케일(사라짐) · A3 스테이지 배경 밀도(Ground 1노드 → 16~19노드) · C4 앰비언트 입자(`village_ambience.gd`) · D3 보스 애니(대상이 브루트→망령으로 바뀌었고 망령은 37프레임).
> **폐기 1건:** D1 코볼트·드레이크 애니 — 두 적은 `data/enemies/`에 없다. `kobold_b.png`·`drake_c.png`는 죽은 에셋이라 「죽은 세대 PNG 정리」로 넘어간다.

### 조명·후처리

- **B1. 풀스크린 비네트 + 색보정** — CanvasLayer + ColorRect + `canvas_item` 셰이더 1장(전 씬 공용). *현황: `assets/shaders/`에 3장 있으나 전부 다른 용도(boss_telegraph·hit_flash — 2장).* — 효과 ★★☆ / 비용 소
- **B2. 씬별 시간대 톤** — 마을=따뜻한 낮(적용됨) · 스테이지1·2=서늘한 숲 · 보스=붉은 어스름. *현황: `CanvasModulate`가 `village.tscn`에만 있다.* — 효과 ★★☆ / **비용 극소**
- **B3. PointLight2D 실광원 실험** — 화덕·모닥불. ⚠ Compatibility 렌더러 + 웹에서 성능 실측 필수, 안 되면 현 가산 글로우 유지. *현황: 0곳.* — 효과 ★★☆ / 비용 중(검증 필요)

### 모션·파티클

- **C1. 모닥불 진짜 불꽃** — 정지 스프라이트+글로우 → 불 파티클(뜨거운 색 그라디언트 + 축소 커브, `preprocess` 필수). 마을·챕터 모닥불 공용 씬으로. *현황: `campfire.tscn`에 파티클 0.* — 효과 ★★☆ / 비용 소
- **C2. 강물 UV 스크롤** — 강 표면 물결 셰이더(UV 스크롤 + 미세 왜곡), 현 반짝 파티클과 조합. — 효과 ★★☆ / 비용 소
- **C3. 바람 — 나무·수풀 흔들림** — `canvas_item` vertex 셰이더로 상단만 좌우 sway(TIME 기반, 나무마다 위상 오프셋). 정지화면이 사라진다. — 효과 ★★☆ / 비용 소
- **C5. 이동 먼지 퍼프 확대** — feel층 플레이어 먼지를 잔몹·NPC에도. *현황: `player.gd`에만.* — 효과 ★☆☆ / 비용 극소

### 애니메이션 커버리지

- 🔴 **브루트 걷기 애니** — `frames` 없이 `sprite` 한 장이라 **정지 자세로 미끄러진다**. 망령 `walk` 누락과 같은 부류이고, 07-27 배치로 처음 화면에 드러났다. (D1을 대체한다 — 원래 대상이던 코볼트·드레이크는 게임에 없다.) — 효과 ★★☆ / 비용 중(아트 위임)
- **D2. NPC idle 숨쉬기 2프레임** — 6종 × 2프레임. 마을이 정지화면에서 벗어난다. *현황: `sprites/village/npc/` 전부 1장짜리.* — 효과 ★★☆ / 비용 소~중

### UI

- **E1. 공용 Theme 리소스** — 기본 회색 Godot 테마 탈피: 패널 9-slice(나무/양피지 톤) · 버튼 상태색 · Galmuri9 일괄. *현황: `.theme`/테마 `.tres` 0개이고 `src/ui/` **6개 패널이 각자 인라인 StyleBox**를 쓴다 — 한 벌로 안 묶여 있다.* — 효과 ★★☆ / 비용 중
- **E2. 힌트 라벨 칩** — 게이트·제작대 "F — " 라벨에 반투명 배경 칩 + 외곽선(가독성). — 효과 ★☆☆ / 비용 극소

### 오디오

- **F1. 마을 앰비언스 루프** — 새소리·강물(gen_sfx 절차 생성 또는 CC0), Audio 오토로드 버스에. *현황: `assets/audio/`에 `sfx/`뿐.* — 효과 ★★☆ / 비용 소
- **F2. BGM** — 마을 / 스테이지 / 보스 3트랙(외부 CC0 검토). ⚠ 웹 autoplay — 첫 입력 후 시작 규약 유지. — 효과 ★★★ / 비용 외부 의존

**추천 순서 (효과 ÷ 비용):** `B2 씬 톤` → `B1 비네트` → `C1 모닥불` → `C2 강물` → `E2 힌트 칩` (전부 비용 소 이하) → 그다음 `브루트 애니` · `E1 테마` · `F1 앰비언스`.
